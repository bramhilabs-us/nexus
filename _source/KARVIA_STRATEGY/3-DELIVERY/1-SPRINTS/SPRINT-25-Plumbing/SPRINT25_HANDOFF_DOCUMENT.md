# Sprint 25 — Handoff Document

<!-- @GENOME T3-SPR-025-HANDOFF | ACTIVE | 2026-05-12 | parent:T3-SPR-025-MP | auto:/init,/close | linked:/sprint-review -->
<!-- Last touched: Day 13 #230 — 🎯 **/close — Sprint 25 SEALED at 45/45 firing (100%)**. All 5 phases shipped: Phase 1 ✅ 11/11 (#209) · Phase 2 ✅ 4/4 (#210 + #212) · Phase 3 ✅ 25/25 (#210-228 across 18 sessions, including the 7-commit Day 12 stretch that landed PX-3.6 macro + PX-3.7 backfill + PX-3.21 cleanup-boundary marking + PX-3.10/11/12/13 regression-rollback-execution-verification chain) · Phase 4 ✅ 1/1 (PX-4.3) · Phase 5 ✅ 4/4 (PX-5.4 #228; PX-5.1 + PX-5.2 + PX-5.3 #230). **Audit IDs touched this session**: 4 — `A20260512-01` NEW (HIGH OPEN, consultant assessment-view auth bug, deferred to post-S25 triage); `A20260506-03` PX-5.1 contract authored 📝→💻; `A20260506-10` PX-5.2 checklist authored 📝→💻; `A20260506-11` PX-5.3 suite shipped 📝→💻→✅ (all three flags). **Major scope amendment honored from #220**: 6 destructive Phase 3 tasks (PX-3.8 + PX-3.14-3.18) deferred to "Sprint Cleanup" phase scheduled post-S28/S29 after Beta launch + 50-100 real-user validation — boundaries marked via CLEANUP-REGISTRY.md + 13 inline `// CLEANUP-TARGET: PX-3.X` markers + npm run audit:cleanup-targets bidirectional cross-check ✅ 13↔6. Sprint firing-task math final: 57 total tasks → 12 deferred (5 from Phase 2 PX-2.5/2.7 + 6 destructive from Phase 3 + 1 absorbed via PX-3.21 add) → **45 firing → 45 sealed**. **Per `audit governance` 3-stage flag state at sprint close**: 📝 plan amendments → ✓ all 11 audit-amended IDs from cross-sprint audit Group 2a/b/3a-d/4a/5a/6/7/8a-d closed at Plan stage; 💻 code shipped → ✓ all 8 code-bearing IDs closed at CODE stage (`A20260506-02/-03/-05/-06/-07/-09/-10/-11` + audit-set `A20260507-01..04` + audit-set `A20260508-01..06` + `A20260510-01`); ✅ tests-green → ✓ all 16 test-bearing IDs sealed (every PX-3.6 a-j + PX-3.6 macro slice 1a/1b/2/3 + PX-3.10 lifecycle + PX-3.7 dry-run + PX-3.7 live integration + PX-5.3 prompt-regression). 4 IDs remain at PLAN-or-CODE (TESTS deferred by design): `A20260506-03` (PX-5.1 grep regression lands with first dispatcher in S26) · `A20260506-10` (PX-5.2 checklist enforces at /deploy time not regression-style) · `A20260506-08` (PX-5.x dispatcher-batch lifecycle digests — owned by S26) · `A20260512-01` (consultant-auth bug — all 3 flags pending post-S25 triage). None of these block S25 close per audit governance ("ID stuck at PLAN-only past target sprint's close = unresolved debt"); each has explicit deferral reason recorded in master tracker. **What S25 unblocks for S26**: (1) `EMAIL_DEEP_LINK_CONTRACT.md` consumed by Workstream B dispatchers (5 dispatchers × 8 URL rows with persona splits + 3-mode token semantics); (2) `notifyTransition()` helper wired into StageTransitionService entry points (PX-2.2 #210); (3) Single-write KR contract enforced cascade-wide — POST/PUT/PATCH all flow through standalone KeyResult collection, embedded `Objective.key_results[]` is read-only fallback for pre-Sprint-22 legacy data only; (4) Legacy WEEKLY backfill script (`migrate-legacy-weekly-to-new.js`) tested + executed against pre-prod (clean no-op) + rollback playbook in hand for prod execution; (5) Cleanup-registry + 13 inline markers so future Sprint Cleanup execution is mechanical; (6) `BETA_LAUNCH_CHECKLIST.md` (39 gates × 6 categories) consumed by /deploy decision after S27 close; (7) Prompt regression suite (`npm run test:prompt-regression`, 504/504 ✓, ~0.18s) gates Beta launch via checklist CQ-3. **Memory rules honored across the 13-session arc**: every destructive task surfaced 2-3 option scope sketch before edit (per `no destructive without greenlight`); FE-shape audit run before PX-3.6 macro slice 1a (caught 11+ FE consumers reading `objective.key_results` from POST responses → reshape helper + virtual landed BEFORE the write drop); A-IDs assigned at audit close + 3-stage flag advance per session (per `audit governance`); model + service schemas read BEFORE seeding fixtures (per `test fixture validation` — caught zero schema-required-field misses across 16 new test suites); user direction "no compromise of safety" 2026-05-10 #217 honored exactly (halt + capture memory before resuming with proper precursor) + "after sprint 25" 2026-05-12 honored (bug logged to tracker, NOT amended into S25); Why → What → How → When framework drove 2 mid-session scope amendments (Phase 2 PX-2.5 demotion at #209 + Phase 3 destructive deferral at #224). **Quality reflection across S25 (16 days, 22 sessions, 230+ session-counter)**: 5/5 phases sealed at firing-task budget without scope creep; 0 destructive operations shipped against production data (all destruction marked as boundaries for future Sprint Cleanup); 16 new regression test suites at 504+ assertions (PX-3.6 a-j + slice 1a/1b/2/3 + PX-3.10 + PX-3.7 dry-run + PX-3.7 integration + PX-5.3 prompt-regression); 4 new governance docs (CLEANUP-REGISTRY + MIGRATION_ROLLBACK_PLAYBOOK + MIGRATION_LOG + REFINEMENT-BACKLOG classification); 2 new technical contracts (EMAIL_DEEP_LINK_CONTRACT + BETA_LAUNCH_CHECKLIST); 2 new services (ObjectiveKeyResultsView for collection-first reads + OnboardingProgressService for state machine + OrphanArchiveService for FK-orphan cleanup, all wired into routes without breaking existing patterns); 1 new audit script (npm run audit:cleanup-targets) + 1 new migration script (npm run migrate:legacy-weekly[:dry-run]). **Next session**: `/strategy` Sprint 26 kickoff (First Objective Created — Stages 0 → 2 activation half). Hard prerequisites all met per S26 master plan: ✓ Phase 1 verification questions answered (#209); ✓ Phase 2 leak fixes shipped (PX-2.6 + PX-2.2 + PX-2.8 + PX-2.9; PX-2.5 + PX-2.7 deferred); ✓ Phase 3 cascade cleanup complete (single-write enforced via PX-3.6 macro + slice 1-3; legacy WEEKLY retired via PX-3.7); ✓ Phase 4 service consolidation resolved (PX-4.3); ✓ `notifyTransition()` helper wired (PX-2.2 #210); ✓ `EMAIL_DEEP_LINK_CONTRACT.md` authored (PX-5.1 #230); ✓ Sprint 25 acceptance criterion verified. S26 first session triage: post-S25 bug `A20260512-01` decision (S26 in-scope, or refinement track); `EMAIL_DEEP_LINK_CONTRACT.md` consumption plan for Workstream B 5 dispatchers; PX-2.5/2.7 deferred items re-evaluation; cohort sizing for Beta-1 per BETA_LAUNCH_CHECKLIST COH-1..6. Day 12 #230 — ✅ PX-5.3 (Prompt Regression Fixture Suite) SEALED, 504/504 ✓. Ships `scripts/test-sprint25-PX5.3-prompt-regression.js` + `npm run test:prompt-regression` (~0.18s runtime). Property-based (NO LLM / network / DB). **30 fixtures** = (25 base matrix: 5 PROMPT_TYPES × 5 maturity stages, structural+identity+absence-of-debug-strings+length+whitespace+idempotence) + (3 per-template interpolation fixtures covering okr_generation/weekly_plan/task_suggestion getUserPromptPrefix keys; single_objective + ssi_narrative don't expose getUserPromptPrefix so no dedicated interpolation row) + (2 edge cases: empty context = placeholders may remain per utils.js:37 contract; stage 99 = falls back to STAGE_OVERLAYS[0] per per-template `||` chain) + (2 invariants: PROMPT_TYPES count+set locked at 5; every type has template with getBasePrompt+getStageOverlay) + (1 error path: unknown prompt type throws with descriptive message). Catches: new prompt type added without fixture (matrix-completeness invariant fails loudly); maturity stage added/removed; base prompt drops "Karvia Coach" identity line; stage overlay loses specialisation; `interpolateTemplate` regression leaves `{{placeholder}}` in output when value supplied; leaked debug strings (TODO/FIXME/XXX/console.log/[DEBUG]); whitespace pathology (4+ consecutive newlines); non-idempotent construction; length pathology (empty / runaway >50K). Explicitly NOT catching: LLM output quality (out-of-scope per audit Group 8b — that's an A/B/eval framework, not regression); prompt effectiveness (telemetry-measured); tone consistency (Group 2c refinement-track, `LLM_TONE_GUIDE.md`). **Useful finding surfaced during build** (logged for refinement track): the 5 templates have **inconsistent context-key support** in their respective `getUserPromptPrefix` builders — `single-objective` has no `getUserPromptPrefix` exported; `weekly-plan` reads only objective/KR/week/date keys; `okr-generation` reads company/industry/employee/period/count; `task-suggestion` reads goal/target/date/count. Surfaced naturally during fixture design (initial attempt assumed `companyName` flows everywhere; it doesn't). Recorded here for refinement-track triage — unification is template-design work, not regression-suite work, so out of scope for PX-5.3. **`/close` skill wiring follow-up** (not in PX-5.3 scope): per audit Group 8b, the suite "runs at every sprint close that touches `server/prompts/`; failure blocks close." Wiring this into `.claude/commands/close.md` / the `/close` skill is a refinement-track item — for now, the npm script is the manual gate, and the BETA_LAUNCH_CHECKLIST.md `CQ-3` cites this suite as a Beta-1 gate. Master tracker `A20260506-11` flipped 📝→💻→✅ in a single session (all three flags = sealed). **Phase 5 firing tasks ALL SEALED** ✅ (4/4: PX-5.4 #228, PX-5.1 + PX-5.2 + PX-5.3 #230). **Sprint 25 firing**: 44/45 (97%). Day 12 #230 (PX-5.2) — ✅ BETA_LAUNCH_CHECKLIST.md SEALED. Ships `KARVIA_STRATEGY/3-DELIVERY/BETA_LAUNCH_CHECKLIST.md` (T2-GOV-011) — single source of truth for Beta-1 launch readiness. 6-category gate matrix with stable codes: **Functional (FUN-1..6)** consolidates S26 + S27 5-verb acceptance into row-per-sprint manual-run gates (both paths consulting + self-serve, all 5 dispatchers, receive-side `<NextStep>` CTAs, 5×5 persona-stage matrix); **Code Quality (CQ-1..7)** lifts single-write enforcement (PX-3.10 38/38), legacy WEEKLY retirement (PX-3.7), prompt regression suite GREEN (PX-5.3), `notifyTransition()` sole entry point, LifecycleTransitionService sole writer, LLMGateway sole AI chokepoint, RBAC lint allow-list; **Perf/Ops (OPS-1..7)** 99.5% Render uptime, 99.9% Mailjet, dailyDigestJob health, KR-aggregation cron alerting, 50-user load smoke, MIGRATION_ROLLBACK_PLAYBOOK ready, APP_URL per-env correctness; **Security (SEC-1..7)** JWT_SECRET fail-fast, multi-tenant isolation lint, XSS discipline, storage-key fragmentation regression (A20260507-01..04), no committed secrets, cross-env link isolation (PX-5.1 contract), `A20260512-01` consultant-auth bug 💻-or-explicit-accept; **Cohort (COH-1..6)** 3-5 named consultants, dry runs complete, <10 companies × <50 users sized, support channel staffed, opt-out path, feedback CTA; **Documentation (DOC-1..6)** 1-page Beta-1 sheet, support runbook, incident response, rollback playbook + Beta-launch section, refinement-backlog classification (PX-5.4), deploy procedure. Each gate row carries concrete pass criterion + owner + verification method. **39 gates total**. Beta-1 scope locked: consulting cohort first, <50 paying users (Beta-2 separate checklist). Sprint-row consolidation table maps every S26/S27 acceptance criterion to a gate above (audit-traceable). Out-of-scope items enumerated (5 deferred-to-Beta-2 + Sprint Cleanup destructive ops + all `nice-after-Beta` items). Sign-off matrix: Eng (FUN/CQ/OPS/SEC) · Product (COH + DOC-1/2/5) · Ops (OPS + SEC-1/5 + DOC-3/6). Final go/no-go consumed by `/deploy` session, not auto-triggered by S27 close. Group 8d (sprint acceptance consolidation) folded into 8a per audit doc line 182. Master tracker `A20260506-10` flipped 📝→💻 (TESTS deferred — checklist enforces gates at deploy time, evidence captured in adjacent `BETA_LAUNCH_EVIDENCE_LOG.md` during launch prep). Doc-only change, zero code risk. **Sprint 25 firing**: 43/45 (96%). Day 12 #230 (PX-5.1) — ✅ EMAIL_DEEP_LINK_CONTRACT.md SEALED. Ships `KARVIA_STRATEGY/2-TECHNICAL/EMAIL_DEEP_LINK_CONTRACT.md` (1-page contract; T2-ARC-022). Locks: (1) `APP_URL` as sole base-URL env var — rejects `BASE_URL`/`FRONTEND_URL`/`SIGNUP_URL` + hardcoded prod-URL fallback (addresses cross-env link bleed surfaced in `JOURNEY_GAP_REPORT_2026-05-08.md`); (2) `/pages/<page>.html` path convention; (3) 7-param canonical query-string grammar — `client` (target company_id, canonical name for consultant cross-tenant view; legacy `?company=`/`?company_id=` aliases read-only), `from`, `source`, `tab`, `objective`, `kr`, `token`; (4) 5-dispatcher × 8-row URL table binding S26 Workstream B send-side ↔ S27 receive-side (covers Profile-complete, Assessment-reminder, Assessment-aggregate, Objective-created, All-KRs-100% — each with persona splits); (5) 3-mode token semantics — invitation (single-use, per-`Invitation.expires_at`) / share (re-readable, per-share TTL) / auth-required (no `?token=`, JWT via `auth-check.js` with `?return=` bounce); explicit "no fourth mode" rule (no fragment tokens, no HMAC signed-URL at v1.0); (6) `<NextStep>` receive-side contract — read params on init, render dispatcher-specific CTA when `source=email`, leave URL params intact, persona-aware via `user.role`; (7) 7 audit-blocking anti-patterns enumerated (hardcoded prod fallback, multiple base-URL env vars, fragment tokens, double-tokenizing, legacy param names in new emails, unencoded values, receiver reading `?token=` for auth). Migration scope: drift in `server/routes/{invitations,consultant,auth,teams,tasks,diagnostic-reports}.js` + `dailyDigestJob.js` is **not** a single-sweep refactor — implementers normalize call-site by call-site as each dispatcher lands in S26/S27. Master tracker `A20260506-03` flipped 📝→💻 (TESTS deferred — regression grep lands with first dispatcher in S26). Doc-only change, zero code risk. **Sprint 25 firing**: 42/45 (93%). Day 12 #228 — ✅ PX-5.4 (REFINEMENT-BACKLOG reorganization) SEALED. Added "## Classification (PX-5.4)" section to `KARVIA_STRATEGY/3-DELIVERY/1-SPRINTS/REFINEMENT-BACKLOG/README.md` splitting all 11 current items into must-before-Beta vs nice-after-Beta. **Verdict: 0 must-before-Beta, 11 nice-after-Beta** — meaningful signal that nothing in this folder gates Beta launch; remaining Beta-gating work lives in Sprint 26 (First Objective) + Sprint 27 (First Task) master plans. Three sub-buckets in nice-after-Beta for navigation: A=6 refinement-track epic drafts (25-3/4/5 + AI tone + field-write symmetry audit + PX-2.7), B=3 post-Beta-1 (self-serve cohort, executive read-only, Mailjet retry), C=4 feature-owning-sprint in-scope items (KR-aggregation × 4 owned by S27, GRIT triggers, AI pilot LLMPolicy, S25 migration-script ownership). Per-item rationale recorded in tables. Existing tables preserved below classification section (additive, no file moves, no destructive edits). "How to handle these drafts" updated with explicit "when adding a new backlog item: classify at the top section + add descriptive entry below" workflow. Genome tag updated to 2026-05-11. Additive doc-only change — no test/code risk. **Sprint 25 firing**: 41/45 (91%). Day 12 #227 — ✅ PX-3.12 + PX-3.13 (Legacy Weekly Migration execution + verification on pre-prod) SEALED. Ran `npm run migrate:legacy-weekly` against `karvia_business_preprod` at 2026-05-12T02:11Z. Outcome: clean no-op exit — script's `candidates.length === 0` guard short-circuited before any DB write, consistent with #222 dry-run finding (pre-prod has zero legacy weekly rows). 6 verification queries all GREEN: V1 (unmigrated candidates) = 0; V2 (stamped sources) = 0; V3 (legacy weekly total) = 0; V4 (WeeklyGoal total) = 1 unchanged; V5 (Move total) = 0 unchanged; V6 (KeyResult sanity) = 11 unchanged. Ships `KARVIA_STRATEGY/3-DELIVERY/MIGRATION_LOG.md` — chronological execution log (one entry today; format covers command + env + SHA + stdout + verification table + verdict + "what this proves / doesn't prove" + implications for Sprint Cleanup prod execution). Cross-linked to CLEANUP-REGISTRY + MIGRATION_ROLLBACK_PLAYBOOK. **What's NOT proven by this run**: live-write code path (runLive + insertMany WG + Task→Move + stamp source) — remains exercised only by mongodb-memory-server integration test (76/76 ✓). Production execution under Sprint Cleanup will be the first live-write exercise against real data. **Sprint 25 firing**: 40/45 (89%). Day 12 #226 — ✅ PX-3.11 (Migration Rollback Playbook) SEALED. Ships `KARVIA_STRATEGY/3-DELIVERY/MIGRATION_ROLLBACK_PLAYBOOK.md` — 5 numbered rollback recipes (PX-3.7 migration / PX-3.8 410-Gone / PX-3.14+3.16+3.17 code-only deletes / PX-3.15 deleteMany / PX-3.18 schema drop) + decision tree (when to invoke) + pre-flight backup procedure (mongodump command for goals/objectives/weeklygoals/moves/tasks/keyresults with timestamp+SHA capture) + per-recipe two variants (transactional script using `_migrated_to` walked-backwards for PX-3.7; restore-from-backup for non-transactional steps) + operational guardrails (observation window, kill-switch re-checks at kickoff/just-before-exec/30min-post, comms protocol). PX-3.18 explicitly flagged HIGHEST-RISK with mandatory pre-execution grep gate (zero embedded readers in routes/services). Cross-linked: CLEANUP-REGISTRY.md now points at playbook from top + "Entries" header; playbook cross-references registry + migrate script + audit script + master plan. Forward-fix preference noted for PX-3.8 (if 410 is doing its job catching missed callers, fix the caller, don't revert). Caveat captured for PX-3.7 rollback: post-migration user edits to WeeklyGoal completions are lost on transactional rollback — must surface to user before invoking. **Sprint 25 firing**: 38/45 (84%). Day 12 #225 — ✅ PX-3.10 (Single-Write Path Regression Suite) SEALED, 38/38 ✓. Adds `scripts/test-sprint25-PX3.10-single-write-regression.js` — **38/38 ✓** end-to-end lifecycle integration test (MongoMemoryReplSet + real Express boot + http requests). Chains POST → GET (list) → PUT /:id/progress → GET → PUT /:id/progress (auto-complete) on the SAME Objective, verifying state at every step. Catches macro regressions that per-slice drift-locks (PX-3.6 slice 1a/1b/2/3 + PX-3.6a-j) miss because they test sites in isolation. Coverage: static wiring (route → service → KeyResult.findByIdAndUpdate; no embedded-mutation idiom regrowth; comment-stripping helper filters benign mentions); 5-stage lifecycle (POST creates 3 KRs in collection / embedded stays []; GET list response.key_results comes from collection via ObjectiveKeyResultsView with _ids matching POST; PUT progress on KR-1 → collection updated to 50, status=in_progress, embedded STILL [], Objective.progress_percentage recomputed >0; GET shows updated KR-1, KR-2/3 untouched; PUT progress on KR-2 to target → status auto-flips to 'completed' via calculatorService, KR-1/3 untouched); collection invariant (exactly 3 KRs throughout — never grew, never duplicated across 5 lifecycle steps); FINAL macro guard (embedded `Objective.key_results.length === 0` after full lifecycle — single-write contract preserved end-to-end). **Sprint 25 firing**: 37/45 (82%). Day 12 #222 — PX-3.7 / slice 1 shipped: `scripts/db/migrate-legacy-weekly-to-new.js` (dry-run scaffold). Day 12 #223 — PX-3.7 slices 2+3 SEALED (live mode + Task→Move). Day 12 #224 — ✅ PX-3.21 (Cleanup Boundary Marking) SEALED. Per user direction 2026-05-11: destructive work (PX-3.8 / 3.14-3.18) deferred to a separate "Sprint Cleanup" phase scheduled post-S28/S29 (after Beta launch + 50-100 real-user validation); meanwhile the Sprint 25 plumbing deliverable is to identify every deletion target and mark its boundary so the future cleanup is mechanical. Ships: (1) `KARVIA_STRATEGY/3-DELIVERY/CLEANUP-REGISTRY.md` — single source of truth, 6 entries (PX-3.8 / 3.14 / 3.15 / 3.16 / 3.17 / 3.18) with file paths, line ranges, reason, prereqs, kill switch, execution order diagram, "revisit this registry" cadence; PX-3.18 expanded to enumerate all 9 fallback-reader sites discovered in sweep (routes/planning.js × 2, routes/moves.js, services/{ObjectiveKeyResultsView,AIContextService,MoveDashboardService,iBrainService,cascade-engine}); (2) 13 inline `// CLEANUP-TARGET: PX-3.X` grep-able markers spanning 11 files — 1 site each for PX-3.8 / 3.14 (combined banner) / 3.16 / 3.17, 9 sites for PX-3.18 (schema + 8 fallback readers; PX-3.15 is data-only, exempt); (3) `scripts/audit-cleanup-targets.js` + `npm run audit:cleanup-targets` — bidirectional cross-check (every marker has a registry entry; every code-kind registry entry has ≥1 marker; multi-ID per line supported via `CLEANUP-TARGET: PX-3.8, PX-3.14` syntax; self-exclude for audit script's own regex). Audit ✅ GREEN: 13 markers ↔ 6 entries verified. **False-positive in sweep**: `routes/ai-okr.js:549/574` reads `AIOKRSuggestion.objectives[].key_results[]` — different model, NOT a PX-3.18 target. Excluded from registry. **Doc-drift fixed inline** (not deferred — pure comment correction, zero risk): `routes/weekly-goals.js:200` no longer misnames PX-3.7; now points at PX-3.18 + cleanup registry. **Master plan updated**: PX-3.8 / 3.14 / 3.15 / 3.16 / 3.17 / 3.18 annotated 🟡 DEFERRED (PX-2.5/2.7-style, NOT dropped). PX-3.21 added to Phase 3 task list (additive plumbing, None deps). Firing-task math: was 50 firing → 6 destructive deferred → +1 PX-3.21 added → **45 firing**. **36/45 firing tasks** sealed (80%). Remaining non-destructive: PX-3.10 (regression test suite), PX-3.11 (rollback doc), PX-3.12 (run migration — no-op on empty pre-prod), PX-3.13 (verification), PX-5.1-5.4 (Phase 5 close deliverables). Per safety-memory update: going forward, destructive tasks default to a deferred cleanup batch rather than interleaving with feature work. -->

**Status**: 🔒 **CLOSED** 2026-05-12 at **45/45 firing (100%)** — sealed Day 13 #230 ([5/5 phases shipped; details in the close comment at line 4 above](#)). Earlier mid-sprint progress notes retained below for audit trail. Day 10 #217 (planning-only, no code shipped): pre-flight code-dependency audit ahead of PX-3.6 destructive macro. Per safety memory `no destructive without greenlight`, ran (a) read-only embedded-vs-standalone KR drift script (`scripts/db/audit-embedded-kr-drift.js`, NEW) on `karvia_business_preprod` — verdict: 11 embedded subdocs across 3 Objectives are pre-PX-3.6i `_id`-divergent from their standalone counterparts but every KR is fully replicated; **data preservation is moot per user direction (test data is disposable)**; (b) grep audit of FK schemas + embedded-subdoc query patterns + `Objective.key_results.id(...)` Mongoose accessors across `server/`/`client/`/`scripts/`. **Findings**: all 4 FK-bearing models (`WeeklyGoal`/`OKROutcome`/`AIInteractionLog`/`Goal`) already point at standalone `KeyResult` (no schema migrations needed). **🚨 Audit gap surfaced**: PX-1.11 (Sprint 25 Phase 1, sealed #209) audited `routes/` only — missed 3 services + 2 model instance methods with embedded reads. Logged as **`A20260510-01`** in [AUDIT_TRACKER.md](../../2-QA-AND-TESTING/AUDIT_TRACKER.md). Amended into Sprint 25 as new ID **PX-3.6j** (services-layer migration batch, 4 sub-slices): (j-1) `services/objectiveService.js:457` `updateProgress` — embedded subdoc mutation → dual-write standalone HYBRID, **LIVE caller** at `PUT /api/objectives/:id/progress`; (j-2) `services/AIContextService.js:1027` `_getFocusContext` — embedded read → standalone-first read with embedded fallback, **LIVE callers** at moves/weekly-goals/ai-okr routes; (j-3) retire dead `services/progress-tracker.js:133` (0 callers); (j-4) retire dead `Objective.addKeyResult` + `updateKeyResultProgress` instance methods (`models/Objective.js:460/500`, defined at PX-3.6a #213, 0 production callers). PX-3.6 + PX-3.18 prereqs widened in master plan to require PX-3.6j ✓. Sprint scope: **49 → 50 firing tasks**. Lesson for future audits: include `services/` + model methods, not just `routes/`. Day 9 #216: PX-3.6h sealed — `weekly-goals.js` GET `/api/weekly-goals/:keyResultId` KR-existence precheck lifted **HYBRID → MIGRATED** (PX-1.11 HYBRID #1 → MIGRATED, the canary). The Sprint 23 #191c embedded `Objective.key_results[]` fallback in the cross-tenant 404 boundary is dropped; the standalone `KeyResult.findOne({_id, company_id})` check is now the sole gate. The `Objective` model `require()` is also dropped (it was imported solely for the retired fallback). Lineage tag `Sprint 25 PX-3.6h` lit in both the file header banner and the GET handler precheck. **22/22 ✓** drift-lock + parse-load smoke (`scripts/test-sprint25-PX3.6h-weekly-goals-kr-read-migration.js`); full 9-test PX-3.6 suite (a-i) regression-green. The Sprint 23 #191 union-read contract (WeeklyGoal + legacy `Goal{WEEKLY}` projection with `_source` tags) is preserved untouched. With weekly-goals.js MIGRATED, the **PX-3.6 macro is unblocked** (drop embedded write at POST routes + drop fallback reads in PX-3.6a-g) and PX-3.18 (drop schema field) follows once PX-3.7 backfills any pre-Sprint-22 embedded-only legacy KRs. **27/49 firing tasks**. Day 8 #215: PX-3.6c objective-wizard.js HYBRID formalization (drift-lock + lineage tag). Day 7 #214: PX-3.6b ai-okr.js (5 sites). Day 6 #213: PX-3.6a objectives.js write-side. Day 5 #212: PX-3.6i + PX-3.6d + PX-3.6f. Day 4 #211: PX-3.6e + PX-3.6d/f resequenced. Day 3 #210: PX-2.8 + PX-2.9 + PX-3.6g. Phase 1 sealed Day 2 #209.
**Sprint Goal**: Fix every leak, retire every redundancy, verify every assumption
**Total Tasks**: **57** (was 47 → 55 at Day 2 #209 → 56 at Day 4 #211 with PX-3.6i added → 57 at Day 10 #217 with PX-3.6j added per `A20260510-01`; net firing 50 after PX-2.5/2.7 deferrals)
**Close Target**: task-driven; estimated ~10 working days from launch

---

## Launch Gate

Sprint 25 starts only when Sprint 24 has shipped:
- [ ] 4-stage `Company.stage` collapse (`prospect/onboarding/active/paused/churned`)
- [ ] 6-stage canonical `Objective.lifecycle_stage` enum (S24 F-1)
- [ ] `LifecycleTransitionService` generic abstraction (S24 F-2)
- [ ] `client/js/display-labels.js` consultant mapping populated (S24 F-1d)

**Verification task**: PX-1.6 must pass before any other Phase 1 work begins.

---

## Progress

| Phase | Tasks | Done | Status |
|---|---|---|---|
| Phase 1 — Discovery | 11 | **11** | ✅ **SEALED #209** ([SPRINT_X_VERIFICATION.md](../../2-TECHNICAL/SPRINT_X_VERIFICATION.md)) |
| Phase 2 — Leak Fixes (post-Phase-1: 4 fire · 5 defer · 1 keep — PX-2.5 demoted #209 + PX-2.7 deferred #210) | **4** firing | **4** | 🟡 In Progress (PX-2.6 + **PX-2.2** + **PX-2.8** + **PX-2.9** ✅; PX-2.5 + PX-2.7 deferred) |
| Phase 3 — Cascade Cleanup (B+C + journey audit + **PX-3.6a-j** consumer migrations + **PX-3.6 macro** + **PX-3.21** cleanup-boundary marking; **PX-3.8 / 3.14-3.18 deferred** to Sprint Cleanup post-S28/S29) | **25 firing** (was 30; -6 destructive deferred per Day 12 #224; +1 PX-3.21 added) | **25 ✅** | 🟡 In Progress (PX-3.19 + PX-3.20 ✅ Day 1; **PX-3.6g** ✅ #210; **PX-3.6e** ✅ #211; **PX-3.6i + PX-3.6d + PX-3.6f** ✅ #212; **PX-3.6a** ✅ #213; **PX-3.6b** ✅ #214; **PX-3.6c** ✅ #215; **PX-3.6h** ✅ #216; **PX-3.6j** ✅ #217; **PX-3.6 macro slice 1 (routes/objectives.js)** ✅ #219; **PX-3.6 macro slice 2 (routes/ai-okr.js × 5 sites)** ✅ #220; **PX-3.6 macro slice 3 (wizard + service)** ✅ #221 — **🎯 PX-3.6 MACRO FULLY SEALED**, 16/16 suites at 504+ assertions; **PX-3.7 slice 1 (dry-run scaffold)** ✅ #222; **PX-3.7 slices 2+3 combined (live mode + Task→Move)** ✅ #223 — **🎯 PX-3.7 SEALED**; **PX-3.21 (cleanup-boundary marking + registry + audit script)** ✅ #224; **PX-3.10 (single-write path regression suite — lifecycle integration)** ✅ #225, 38/38 ✓; **PX-3.11 (migration rollback playbook)** ✅ #226; **PX-3.12 + PX-3.13 (preprod migration execution + verification)** ✅ #227 — clean no-op, MIGRATION_LOG.md shipped; **Phase 3 firing tasks ALL SEALED** ✅) |
| Phase 4 — Service Consolidation (post-Phase-1: 1 fire · 1 defer · 1 keep) | **1** firing | **1** | ✅ PX-4.3 ✅ retired |
| Phase 5 — Sprint-Close Deliverables (PX-5.1–5.4 mandatory) | 4 | **4** | ✅ **ALL FIRING SEALED** — **PX-5.4** ✅ #228 (REFINEMENT-BACKLOG classified must/nice); **PX-5.1** ✅ #230 (EMAIL_DEEP_LINK_CONTRACT.md shipped); **PX-5.2** ✅ #230 (BETA_LAUNCH_CHECKLIST.md shipped); **PX-5.3** ✅ #230 (prompt regression suite: 30 fixtures, 504/504 ✓, 0.18s) |
| **Total firing** | **45** (was 50 → 6 destructive deferred + 1 PX-3.21 added #224) | **45** | ✅ **100% — SPRINT 25 SEALED** at /close #230 (Day 13, 2026-05-12) |

### Day-1 Kickoff Decisions (locked 2026-05-08, Session #208)

| Decision | Source Task | Outcome |
|---|---|---|
| `ssi-questions-library.json` path | PX-2.7 | ✅ **RETIRE** — delete JSON + `seed:questions` + `seed:templates`; `seed:mece` becomes sole canonical seed path. Rationale: 82% fail MECE enum; sprint thesis "retire every redundancy"; salvageable 18% can be re-authored if missing. |
| Orphan `assessment_templates` strategy | PX-2.9 | ✅ **ARCHIVE** — add `archived_at + archived_by_user_id` fields; `User.pre('deleteOne')` + `Company.pre('deleteOne')` set flags, do not delete. Rationale: aligns with codebase soft-delete house style; 2-field schema cost; preserves audit trail. |
| Phase B migration open Qs | PX-3.2 | ✅ **All picks confirmed** — frequency default `'once'` (most conservative); Tasks → convert to Moves (preserve effort); `progress_pct` seed `0` (clean slate). |

### Day-1 Early-Bird Unblockers (no Phase 1 dependency, fire in parallel)

| ID | Task | Why early |
|---|---|---|
| PX-3.20 | `Goal.week` conditional `required` | Blocks QUARTERLY goal creation today; downstream tests cannot set up valid fixtures otherwise |
| PX-3.19 | Goals route field-name unify (`name` ↔ `title`) | Pure consolidation, no dependencies |
| PX-2.6 | MECE seeder `is_active:true` default fix | CRITICAL journey-blocker (`A20260508-01`) |

### Phase 1 Outcomes (sealed 2026-05-09, Session #209)

Full report: [SPRINT_X_VERIFICATION.md](../../2-TECHNICAL/SPRINT_X_VERIFICATION.md). Spike doc: [KR_AGGREGATION_AND_LIFECYCLE_AUDIT.md](../../2-TECHNICAL/KR_AGGREGATION_AND_LIFECYCLE_AUDIT.md).

**Conditional task decisions**:

| Task | Phase 1 finding | Decision |
|---|---|---|
| PX-2.1 (auto-fire SSI narrative on Assessment.post-save) | Lazy-on-read works; not a Beta blocker | 🟡 **DEFER → refinement track** |
| PX-2.2 (`notifyTransition()` inline helper) | No helper exists today; per-route `fireAfterWriteEvaluator()` is closest pattern | ✅ **FIRE** |
| PX-2.3 (define `showEmptyState()`) | Already defined `objectives.js:662`; called at 4 sites | ❌ **NOT NEEDED** |
| PX-2.4 (build AIOKRSuggestion review UI) | Full surface exists (`ai-okr-review.html` + 4 endpoints) | ❌ **NOT NEEDED** |
| PX-2.5 (FE wire-up for KR regen on standalone objectives page) | BE endpoint is **wizard-session-bound**, NOT usable for saved Objectives — needs new endpoint | 🟡 **DEFER to own session** (BE+FE+tests ≈ 2-3h, not the small wire-up originally scoped) |
| PX-4.1 (retire `AIObjectivePlanner`) | Single-purpose, 1 caller; consolidatable but low value | 🟡 **DEFER → refinement track** |
| PX-4.2 (retire `/api/ai-okr/*`) | NOT redundant with `/api/objective-wizard/*` (distinct UX flows) | ❌ **KEEP — formally retain** |
| PX-4.3 (retire `OKRRecommendationService`) | Dead — only `testSSIDiagnostic.js:38` references it | ✅ **FIRE** |

**🚨 Critical finding from PX-1.11** — 8 files still read embedded `Objective.key_results[]`:

| File | Status |
|---|---|
| `routes/objectives.js` | NOT-MIGRATED |
| `routes/ai-okr.js` | NOT-MIGRATED |
| `routes/objective-wizard.js` | NOT-MIGRATED |
| `routes/consultant.js` | NOT-MIGRATED |
| `routes/cascade.js` | NOT-MIGRATED |
| `routes/planning.js` | NOT-MIGRATED |
| `routes/moves.js` | NOT-MIGRATED |
| `routes/weekly-goals.js` | ✅ **MIGRATED** (PX-3.6h #216, Day 9) |

**PX-3.6 + PX-3.18 are NOT SAFE without first migrating these 8 consumers.** **Locked Day 2 #209** (user decision): formalize as 8 separate IDs **PX-3.6a–PX-3.6h** in Phase 3 (one per file) before PX-3.6. PX-3.6 (drop embedded write at POST sites) and PX-3.18 (drop schema field) prereqs updated to require ALL of a-h ✓ first. Sprint scope: 47→55 tasks. See [SPRINT25_MASTER_PLAN.md Phase 3](SPRINT25_MASTER_PLAN.md#phase-3--cascade-cleanup-phase-b--phase-c-combined) for IDs.

**🚨 Day 10 #217 amendment — PX-1.11 scope gap (`A20260510-01`)**: pre-flight code-dependency audit found PX-1.11 walked `routes/` only — missed `services/` + model instance methods. 3 services + 2 model methods with embedded reads:

| File | Type | Caller status | Hazard if PX-3.6 ships without fix |
|---|---|---|---|
| `services/objectiveService.js:457` (`updateProgress`) | mutates embedded subdoc directly | LIVE — `routes/objectives.js:959` (`PUT /api/objectives/:id/progress`) | **WRITE drift** — embedded mutated, standalone goes stale |
| `services/AIContextService.js:1027` (`_getFocusContext`) | reads via `Objective.findOne({'key_results._id'})` | LIVE — `routes/moves.js:492`, `routes/weekly-goals.js:425`, `routes/ai-okr.js` | **READ stale** — LLM context from frozen embedded |
| `services/progress-tracker.js:133` (`trackKeyResultUpdate`) | reads via `.key_results.id(...)` | DEAD — 0 callers | None (retire) |
| `models/Objective.js:460` (`addKeyResult` instance) | `this.key_results.push(...)` | DEAD — 0 production callers | None (retire) |
| `models/Objective.js:500` (`updateKeyResultProgress` instance) | mutates via `.key_results.id(...)` | DEAD — 0 production callers | None (retire) |

Amended into Sprint 25 as **PX-3.6j** (services-layer migration batch, 4 sub-slices j-1..j-4 in master plan). PX-3.6 + PX-3.18 prereqs widened to require PX-3.6j ✓. Sprint scope: 55→57 tasks (49→50 firing).

**Refinement-track items captured** (NOT in Sprint 25 scope):
- PX-1.2 outcome: SSI narrative is lazy-on-read; consider eager fire if dashboard load profile changes
- PX-1.8 outcome: 12-block DiagnosticReport fetched into context but not exposed to refine-objective LLM prompt
- PX-1.10 outcome: `aiOKRService` + `AIObjectivePlanner` consolidation candidate
- KR_AGGREGATION_AND_LIFECYCLE_AUDIT G1: Sprint 27 owns KR-aggregation cron design (no scheduler today)

---

## Acceptance Criterion

See [SPRINT25_MASTER_PLAN.md §Acceptance Criterion](SPRINT25_MASTER_PLAN.md).

Summary:
- [ ] All Phase 1 tasks complete; verification doc populated
- [ ] All Phase 3 tasks complete (Phase B + Phase C combined)
- [ ] Phase 2 + Phase 4 conditional tasks resolved (built or formally deferred)
- [ ] Single-write cascade enforced; legacy collections deleted
- [ ] `notifyTransition()` helper wired into `StageTransitionService` entry points (no event-bus / listener-registry per Cross-sprint audit Group 2a)
- [ ] `EMAIL_DEEP_LINK_CONTRACT.md` authored (Group 2b)
- [ ] `BETA_LAUNCH_CHECKLIST.md` authored (Group 8a)
- [ ] Prompt regression fixture suite built (Group 8b)
- [ ] `REFINEMENT-BACKLOG/` split into must-before-Beta / nice-after-Beta (Group 8c)
- [ ] Regression test suite green

---

## Architectural Invariants (verify at every session close)

- [ ] Zero executable `switch-company` callers in `client/pages/scripts/`
- [ ] LLMGateway sole executable OpenAI chokepoint
- [ ] All consultant data access goes through `requireManagedClient`
- [ ] StageTransitionService sole writer of `Company.stage`
- [ ] LifecycleTransitionService sole writer of `Objective.lifecycle_stage`
- [ ] No new role-check sites outside phase3-3 lint allow-list
- [ ] **NEW**: Cascade single-writer — `WeeklyGoal` sole weekly write target; `KeyResult` sole KR write target

---

## Watch-Items

Carried from prior sprints + four-pass audit:
- KR-creation flow needs verification (Session A discovery)
- Enrich endpoint needs diagnosis (S22 watch-item carried)
- 6 open verification questions all addressed in Phase 1

---

## Open Decisions (resolve at sprint kickoff or during execution)

- **Phase B open questions** (per CONSOLIDATION_PLAN.md):
  1. Frequency mapping default for legacy → new migration
  2. Task vs Move handling for legacy weekly Goals
  3. Progress percentage seeding policy
- These get answered in PX-3.2 by the migration-script author with eyes on actual data shapes.

---

## Carry-Forward to Sprint 26 (will be filled at sprint close)

- `notifyTransition()` helper (Sprint 26 lands 4 transition emails on top — no separate dispatcher subscribers)
- `EMAIL_DEEP_LINK_CONTRACT.md` (Sprint 26 send-side + Sprint 27 receive-side both consume)
- `BETA_LAUNCH_CHECKLIST.md` (Sprint 26 + Sprint 27 reference for acceptance gating)
- Prompt regression fixture suite (Sprint 26 + Sprint 27 must run before close)
- KR-aggregation formula (documented; Sprint 27 consumes)
- Verified empty-state / approval-workflow / notification status

---

## Audit History

Audit IDs that amended this sprint. Update flags as work advances. Master tracker: [AUDIT_TRACKER.md](../../2-QA-AND-TESTING/AUDIT_TRACKER.md).

| ID | Source | One-liner | 📝 | 💻 | ✅ |
|---|---|---|---|---|---|
| `A20260506-02` | Cross-sprint audit S24-S27, Group 2a | Inline `notifyTransition()` helper in PX-2.2 (replaces "StageTransitionDispatcher") | ✓ | ✓ | — |
| `A20260506-03` | Cross-sprint audit S24-S27, Group 2b | Author `EMAIL_DEEP_LINK_CONTRACT.md` at sprint close (~1 hr, 1 page) | ✓ | — | — |
| `A20260506-09` | Cross-sprint audit S24-S27, Group 6 | New PX-1.11 consumer audit task in Phase 1; PX-3.6 / PX-3.18 prereq-linked | ✓ | — | — |
| `A20260506-10` | Cross-sprint audit S24-S27, Group 8a | Author `BETA_LAUNCH_CHECKLIST.md` at sprint close (~2-3 hrs) | ✓ | — | — |
| `A20260506-11` | Cross-sprint audit S24-S27, Group 8b | Build prompt regression fixture suite at sprint close (+2 pts) | ✓ | — | — |
| `A20260506-12` | Cross-sprint audit S24-S27, Group 8c | Split `REFINEMENT-BACKLOG/` into must-before-Beta / nice-after-Beta | ✓ | — | — |
| `A20260506-13` | Cross-sprint audit S24-S27, Group 7 | Lifecycle writer mirrors `StageTransitionService` pattern (invariant added to master plan) | ✓ | — | — |
| `A20260508-01` | Journey Smoke Test 2026-05-08 (Session #207) | **CRITICAL** — MECE seeder `is_active:true` default fix (PX-2.6); journey-blocker out-of-box | ✓ | ✓ | — |
| `A20260508-02` | Journey Smoke Test 2026-05-08 (Session #207) | **HIGH** — Retire `ssi-questions-library.json` + `seed:questions` + `seed:templates` paths in favor of `seed:mece` (PX-2.7); 82% of JSON unseedable today. **Deferred Day 3 #210** to refinement backlog: not a Beta blocker (no Beta path triggers `seed:questions`); Day-1 RETIRE strategy decision still stands when refinement track picks it up. | ✓ | — | — |
| `A20260508-03` | Journey Smoke Test 2026-05-08 (Session #207) | MEDIUM — Goals route field-name unify (`name` ↔ `title`) on sibling endpoints (PX-3.19) | ✓ | ✓ | — |
| `A20260508-04` | Journey Smoke Test 2026-05-08 (Session #207) | **HIGH** — `Goal.week` conditional `required` based on `time_period` (PX-3.20); QUARTERLY goals reject valid input | ✓ | ✓ | — |
| `A20260508-05` | Journey Smoke Test 2026-05-08 (Session #207) | **HIGH** — Onboarding state machine wiring (PX-2.8): `Company.onboarding_progress` ticks + `Company.stage` transitions never fire today; couples with PX-2.2 `notifyTransition()` | ✓ | ✓ | — |
| `A20260508-06` | Journey Smoke Test 2026-05-08 (Session #207) | MEDIUM — `assessment_templates` orphan cleanup hooks on user/company delete (PX-2.9); 83 dead templates pre-wipe | ✓ | ✓ | — |

**Workflow**: when implementing in `/coding`, set 💻 here AND in master tracker; cite ID in commit message. At sprint close `/close`, set ✅ once regression suite green.

---

## Sign-off

Sprint 25 handoff initialized 2026-05-06. Planning complete. Sprint launches when S24 prereqs ship.

**Amendment 2026-05-08 (Session #207)**: 6 audit IDs (`A20260508-01..06`) added from Journey Smoke Test against `karvia-business-1` Render Dev. S24 prereq gate confirmed GREEN at #199 + #206b walkthrough. S25 cleared to launch with the expanded scope (~39 tasks, was ~33). Findings doc: [JOURNEY_GAP_REPORT_2026-05-08.md](../../2-QA-AND-TESTING/JOURNEY_GAP_REPORT_2026-05-08.md).

**Day 1 launch 2026-05-08 (Session #208)**: 3 kickoff strategy decisions locked (PX-2.7 RETIRE, PX-2.9 ARCHIVE, PX-3.2 all picks). 3 early-bird unblockers shipped — PX-3.20 (`Goal.week` conditional required + production `week:1` workaround sweep across `planning.js`), PX-3.19 (goals route accepts `title` OR `name` on POST/PUT/bulk; canonical = `title`), PX-2.6 (`AssessmentQuestion.is_active` field added + seeder explicit `is_active:true` for upsert path). Behavior assertions green: QUARTERLY no-week ACCEPTED, WEEKLY no-week REJECTED, `is_active` defaults true. Regression: S24 #244 plan-tab 75/75 ✓, S23 #191 planning-page 135/135 ✓.

**Day 11c — 🎯 PX-3.6 MACRO SEALED: slice 3 of 3 shipped 2026-05-11 (Session #221)**: Final slice closing the PX-3.6 macro. Same-day continuation of #219 (slice 1) + #220 (slice 2). Slice 3 (commit `50336a8`) drops the embedded `Objective.key_results[]` write at the last two surfaces: `routes/objective-wizard.js` finalize + `services/objectiveService.js` `updateProgress`. **Wizard refactor**: mechanical mirror of slice 1b — `embeddedKRs` identifier retired and replaced by `krSpecs` that generates `_id` upfront via `new mongoose.Types.ObjectId()`; Objective ctor takes `key_results: []`; `KeyResult.insertMany` sources `krDocs` from `krSpecs` (SOLE writer); response `key_result_ids` sources from `krSpecs.map(kr => kr._id)`; `mongoose` import added; lineage comments preserved as 4-link chain (Sprint 23 Epic E + PX-3.6c + PX-3.6i + slice 3); non-blocking warn updated to "collection write failed" (was "dual-write failed"). **Service `updateProgress` refactor** (mutation-flow variant — was PX-3.6j-1 HYBRID): `objective.key_results.id(update.keyResultId)` embedded lookup RETIRED; KRs loaded from collection via `KeyResult.find({objective_id, status:≠cancelled}).lean()`; in-memory `updated` plain object holds the post-update KR shape; status derivation runs on the plain object via `calculatorService.calculateKeyResultProgress` + `isAtRisk` (calculators read only `{metric_type, current_value, target_value}` so they work on lean docs without Mongoose subdoc methods); `KeyResult.findByIdAndUpdate` is now the SOLE persist path (was HYBRID alongside embedded mutation); `objective.progress_percentage` recomputed from `postUpdateKRs` (post-update list carrying forward unchanged KRs so the average reflects the full KR set, not just the updated ones); `objective.save()` still runs to persist only `progress_percentage` — the empty `key_results[]` array is untouched; passes `postUpdateKRs` to `enrichObjectiveData(objective, override)`. **`enrichObjectiveData` extended**: added optional 2nd parameter `keyResultsOverride = null`. When provided, uses it for KR stats + enrichment; otherwise falls back to `objective.key_results` (preserves the existing behavior of 5 other callers at L73, L424, L544, L572, L625 — none of which are slice 3's scope; documented as bounded regression that PX-3.18 forces full migration of). **NEW** [`scripts/test-sprint25-PX3.6-slice3-wizard-and-service-drop-embedded.js`](../../../scripts/test-sprint25-PX3.6-slice3-wizard-and-service-drop-embedded.js) **35/35 ✓** in 3 sections: [1] wizard static (12 — `embeddedKRs` retired, `krSpecs` build present, KR `_id` upfront, `key_results: []` ctor, krDocs from krSpecs, key_result_ids from krSpecs, `mongoose` import, slice 3 lineage ≥2×, non-blocking warn wording updated, legacy warn wording retired); [2] service static (9 — embedded mutation retired, `KeyResult.find` collection-sourced lookup, `postUpdateKRs` array, `calculateObjectiveProgress(postUpdateKRs)`, `KeyResult.findByIdAndUpdate` SOLE persist, `enrichObjectiveData` accepts override, override usage, `updateProgress` passes `postUpdateKRs`, slice 3 lineage ≥3×); [3] service runtime (14 — seeded slice-3-shape objective with empty embedded + 2 collection KRs; first `updateProgress` call pushes KR-A to 50% → collection KR-A current_value=50, status=in_progress, KR-B untouched, embedded stays empty, `progress_percentage` recomputed to (50+0)/2 = 25; enriched response via override has 2 KRs with progress 50/0; second call pushes KR-A to 100 → status flips to completed, `progress_percentage` = (100+0)/2 = 50, embedded STILL empty; backward-compat: `enrichObjectiveData` with no override reads `objective.key_results`). **Updated [`scripts/test-sprint25-PX3.6c-objective-wizard-write-migration.js`](../../../scripts/test-sprint25-PX3.6c-objective-wizard-write-migration.js) 22/22 ✓** — HYBRID dual-write assertions inverted to slice 3 contract; block extractor anchors on "KeyResult collection is the SOLE write"; gate `if (krSpecs.length)`; map `krSpecs.map`; try/catch errname `krWriteErr`; warn wording "collection write failed"; `key_results: embeddedKRs` survivor check (must be absent); lineage chain (Sprint 23 Epic E + PX-3.6c + PX-3.6i) preserved as discrete checks. **Updated [`scripts/test-sprint25-PX3.6j-1-objectiveService-updateProgress-mirror.js`](../../../scripts/test-sprint25-PX3.6j-1-objectiveService-updateProgress-mirror.js) 19/19 ✓** — HYBRID mirror assertions inverted to slice 3 collection-only contract; `krMirrorPayloads.push` captures from `updated` plain object (was `kr` post-Mongoose-subdoc-mutation); embedded mutation idioms (`kr.current_value = update.currentValue`, `objective.key_results.id(update.keyResultId)`) asserted as RETIRED; new contract markers added: `KeyResult.find` collection-sourced lookup, `postUpdateKRs` array, `calculateObjectiveProgress(postUpdateKRs)`. **Updated [`scripts/test-sprint25-PX3.6i-kr-id-passthrough.js`](../../../scripts/test-sprint25-PX3.6i-kr-id-passthrough.js) 30/30 ✓** — wizard-side assertions retargeted to slice 3 supersession (same pattern as objectives.js superseded by slice 1b); legacy `newObjective.key_results.map` for krDocs asserted as RETIRED; krDocs from upfront-generated `krSpecs`; PX-3.6i OR slice 3 citation acceptable. **Final regression sweep — 16/16 suites all green at 504+ assertions, 0 failed**: PX-3.6a 32/32 · b 56/56 · c 22/22 · d · e 30/30 · f 14/14 · g 20/20 · h 22/22 · i 30/30 · j-1 19/19 · j-2 16/16 · j-3 20/20 · slice 1a 51/51 · slice 1b 44/44 · slice 2 26/26 · slice 3 35/35. **PX-3.6 MACRO STATUS: ✅ FULLY SEALED**. Every embedded `Objective.key_results[]` write surface is retired across the codebase: routes/objectives.js ✅ (slice 1) · routes/ai-okr.js ✅ (slice 2) · routes/objective-wizard.js ✅ (slice 3) · services/objectiveService.js ✅ (slice 3). The standalone `KeyResult` collection is now the SOLE writer of KR data. **Why-first audit (3 Qs)**: Q1 bundle wizard + service in one commit — both are the LAST two write surfaces; splitting buys nothing; slice 2 proved bundle-not-split at 5 sites. Q2 plain `updated` object for status derivation vs Mongoose subdoc mutation — calculatorService is pure-function-style (reads only `{metric_type, current_value, target_value}`); avoiding Mongoose subdoc machinery removes a class of "did this subdoc persist?" bugs; persist itself goes through `findByIdAndUpdate` which speaks Mongo directly. Q3 `keyResultsOverride` param vs making enrichObjectiveData async to fetch via helper — sync function called from 6+ sites with varying KR-source semantics; making async cascades caller changes (too risky); override param is minimal-change extension that fixes slice 3's caller without disturbing others (documented bounded regression for other callers that PX-3.18 forces a full migration of). **Memory rules honored**: per `feedback_no_destructive_without_greenlight`, surfaced the scope + bounded regression in `enrichObjectiveData` BEFORE the first edit; per `Stay grounded — minimal change`, re-used the helper + extended `enrichObjectiveData` minimally (1 param + 1 line of fallback); per `Why → What → How → When`, the why-first cascade (drift elimination → 2 last surfaces → mechanical mirror + mutation-flow rewrite → now because S26 First-Objective depends on macro sealed) drove the decision-tree at every fork. **Quality reflection: 9/10**. Strong: 35-assertion slice 3 test passed first-run on the runtime path (proves the in-memory `updated` shape + collection lookup + progress recompute work as designed); test inversions on PX-3.6c, PX-3.6j-1, PX-3.6i all surgical (no test-architecture rewrites); chose to extend `enrichObjectiveData` instead of touching 5 unrelated callers (per minimal-change discipline). Negative: PX-3.6i wizard-side supersession check was missed in the initial sweep (caught by full regression); should have been caught by reading PX-3.6i during slice 3 planning — same supersession was already applied to objectives.js section. **Audit IDs**: PX-3.6 macro `A20260506-09` (parent PX-1.11) FULLY ✓ across all sub-trackers: objectives.js + ai-okr.js + objective-wizard.js + objectiveService.js. **Next**: PX-3.7 (`scripts/db/migrate-legacy-weekly-to-new.js` idempotent backfill with `--dry-run`) is now safe to schedule — no new writes go to embedded so legacy embedded-only KRs can be backfilled without race. After PX-3.7 ✓ + PX-3.8-3.17 legacy retirement, PX-3.18 (schema-field drop) is the final step. Phase 5 sprint-close (PX-5.1–5.4) still pending.

**Day 11b — PX-3.6 macro slice 2 of 3 for `routes/ai-okr.js` (5 sites) shipped 2026-05-11 (Session #220)**: Same-day continuation of session #219 after slice 1 sealed `routes/objectives.js`. Slice 2 (commit `747b739`) ships the mechanical mirror across all 5 KR-creation POST routes in `ai-okr.js` — single commit (bundled, not per-site) because the sites are mechanically identical and slice 1b runtime-proved the pattern at the analogous POST `/api/objectives`. Sites migrated: **Site 1** `POST /api/ai-okr/approve` (legacy approve via `AIOKRSuggestion`, [`ai-okr.js:649`](../../../server/routes/ai-okr.js#L649)); **Site 2** `POST /api/ai-okr/approve-draft` (active approve, [`ai-okr.js:1055`](../../../server/routes/ai-okr.js#L1055)); **Site 3** `POST /api/ai-okr/generate-from-company` (SOLE cascade-acceptance writer per PX-1.11 audit, [`ai-okr.js:2376`](../../../server/routes/ai-okr.js#L2376)); **Site 4** `POST /api/ai-okr/generate-single-objective` AI-disabled template fallback ([`ai-okr.js:2645`](../../../server/routes/ai-okr.js#L2645)); **Site 5** `POST /api/ai-okr/generate-single-objective` AI-enabled LLM main path ([`ai-okr.js:3170`](../../../server/routes/ai-okr.js#L3170)). **Per-site refactor** (mechanical mirror of slice 1b): each site builds a local `krSpecs` array, generating each KR's `_id` upfront via `new mongoose.Types.ObjectId()`; the Objective constructor takes `key_results: []` (embedded stays empty); `KeyResult.insertMany` sources `krDocs` from `krSpecs` and is the SOLE write target; failure remains non-blocking with the warn wording updated to `[AI OKR Routes] KeyResult collection write failed (non-blocking)` (was `dual-write failed`). **Sites 4 + 5 also wire response shape**: these are the only 2 sites that return `key_results` in the response (sites 1, 2, 3 return only `objective_ids` / `createdObjectives` metadata). Response handling: `objective.toJSON()` preserves Objective virtuals (e.g. `sustained_eligible` for `client-workspace.js:647`), then helper-served KR array overlays the `key_results` field via `ObjectiveKeyResultsView.forObjective(...)` so FE doesn't see an empty array on new objectives. Site 4 also handles the `...templateObjective` spread carefully: `{ key_results: _stripKRs, ...templateRest }` destructures the templateObjective's KRs out before spreading, then reconstitutes them upfront as `krSpecs` — prevents the spread from reintroducing an embedded array post-refactor. **Imports**: `const mongoose = require('mongoose')` added at top of file for `new ObjectId()`; `const ObjectiveKeyResultsView = require('../services/ObjectiveKeyResultsView')` added for sites 4 + 5's response shape. NEW [`scripts/test-sprint25-PX3.6-slice2-ai-okr-drop-embedded.js`](../../../scripts/test-sprint25-PX3.6-slice2-ai-okr-drop-embedded.js) **26/26 ✓** in 3 sections: [1] deleted idioms (5 — no `<source>.key_results.map` ctor survivors, no `krDocs = objective.key_results.map` survivors, legacy "HYBRID dual-write" wording retired, 5 `key_results: []` ctor inits, 5 `krDocs = krSpecs.map` builds); [2] new contract markers (6 — mongoose import, helper import, ≥10 slice 2 lineage citations, 5 `krSpecs =` builds, 5 upfront `_id: new mongoose.Types.ObjectId()` generators, 2 `ObjectiveKeyResultsView.forObjective(` calls at sites 4 + 5); [3] runtime site 4 AI-disabled (15 — `FEATURE_OPENAI_ENABLED=false` forces template path; POST 200; `ai_used:false`; helper-served response shape; 3 KRs in response with `quarter` singular; persisted `Objective.key_results[]` is EMPTY; 3 collection rows; `_id`s match response; `quarters` array shape correct; `year` matches `target_year`; `created_by` + `ai_generated:true` invariants). **Updated [`scripts/test-sprint25-PX3.6b-ai-okr-write-migration.js`](../../../scripts/test-sprint25-PX3.6b-ai-okr-write-migration.js) 56/56 ✓** — HYBRID dual-write assertions inverted to slice 2 contract: block extractor finds slice 2 SOLE-write blocks (was HYBRID-tagged); block gate `if (krSpecs.length)` (was `objective.key_results.length`); krDocs source `krSpecs.map` (was `objective.key_results.map`); try/catch errname `krWriteErr` (was `dualWriteErr`); warn wording "collection write failed" (was "dual-write failed"); Site 3 SOLE-writer citation slice 2 wording; section [3] retired the "embedded write preserved (HYBRID posture)" checks and replaced with "collection-only posture" — 5 upfront ObjectId builds, 5 `key_results: []` ctor inits, 0 ctor-arg survivors, every block sits AFTER `await objective.save()`. **Why-first audit (3 Qs)**: Q1 bundle vs per-site — sites are mechanical mirrors of slice 1b; slice 1b runtime-proved the pattern at the canonical POST `/api/objectives`; Site 3 is the SOLE cascade-acceptance writer and half-migrating creates inconsistent state across the very writer that exists to prevent drift. Q2 static-only for sites 1, 2, 3, 5 — site 4 is the only one reachable without LLM/SSI/Suggestion fixture scaffolding; static drift-lock for the rest mirrors PX-3.6b's static-only rationale and is justified by slice 1b's full runtime proof at the mechanically-identical POST `/api/objectives` (47 runtime assertions in slice 1b's A/B/C scenarios). Q3 preserve `[AI OKR Routes]` log prefix vs slice-2 prefix — `[AI OKR Routes]` is the file-scope log prefix used throughout for ANY message from this router (success + warn + error); adopting a slice-specific prefix would break log aggregation. Wording change "dual-write failed" → "collection write failed" reflects SOLE-writer posture. **Memory rules honored**: per `feedback_no_destructive_without_greenlight`, surfaced the scope + dependency + value reflection BEFORE first edit (user explicitly requested the impact/dependencies/value reframing); the 3-question reflection yielded the bundle-not-split decision Q1. Per `Stay grounded — minimal change` — re-used the helper from slice 1a (no new infrastructure); the response-shape wiring at sites 4 + 5 is byte-identical to slice 1a's `.toJSON()` + helper-overlay pattern; even the templateObjective spread fix is a single destructure rather than a full template-generator rewrite. Per `Why → What → How → When`, the reflection pause established Why (SOLE cascade-acceptance writer drift hazard) before What (5-site bundle) before How (helper mirror) before When (now, because S26 First-Objective depends on a single-writer schema). Per `feedback_audit_governance`, every site has its own slice 2 citation comment + a single drift-lock test pinning all 5. **Quality reflection: 9/10**. Strong: 5-site bundle landed on first-run test pass; per-site comments cite the SOLE-writer status of site 3 explicitly to flag downstream readers; templateObjective spread bug caught + fixed during code-write (before test) by reading the templateObjective return shape before editing; PX-3.6b test inversion was surgical (5 line-level changes to assertions, no test-architecture rewrite); helper re-use proved the slice 1a investment was directionally correct (no new infrastructure needed for slice 2). Negative: missed a backtick collision in one template-literal label (\`_id\` inside a backtick-bounded label string — JS parser caught it on first run; trivial fix); should have anticipated the `objective.key_results.length` check at sites 1-3 wouldn't exist anymore (gate is now `krSpecs.length`, was caught by the test's first runtime pass on site 4). **Audit IDs**: PX-3.6 macro `A20260506-09` (parent PX-1.11) — slice 2 closes the `routes/ai-okr.js` row in `SPRINT_X_VERIFICATION.md` consumer migration table. **Next session**: `/coding` slice 3 — `routes/objective-wizard.js` finalize + `services/objectiveService.js:464` `updateProgress`. Wizard is mechanical mirror of slice 1b POST; `updateProgress` is a mutation flow (different shape — was PX-3.6j-1 HYBRID). After slice 3 ✓, PX-3.6 macro seals → PX-3.7 backfill → PX-3.18 schema-field drop. Phase 5 sprint-close (PX-5.1–5.4) still pending.

**Day 11 — PX-3.6 macro slice 1 of 3 for `routes/objectives.js` shipped 2026-05-11 (Session #219, 2 commits)**: First of three slices of the PX-3.6 macro (drop embedded `Objective.key_results[]` dual-write). Scoped tightly to `routes/objectives.js`; slices 2 (`ai-okr.js` × 5 sites) + 3 (`objective-wizard.js` + `services/objectiveService.js`) deferred to next session. Two commits inside the session for rollback granularity: **slice 1a `413f03c`** = precursor safety net (additive, no behavior change); **slice 1b `9175cd5`** = destructive drop. **Why split the macro into 3 slices instead of one**: per `Stay grounded — minimal change` + `no destructive without greenlight` memory rules, surfaced scope first to user; mid-session FE audit revealed 11+ FE pages read `objective.key_results` from GET response (`objectives.js`, `planning-v2.js`, `objective-detail.js`, `objective-calculator.js`, `team-ssi-view.js`, `ai-okr-review.js`, `client-workspace.js`, `okr-creation-wizard.js`, `ssi-report-full.js`, `business-assessment.js`, `objectives.html`) and 3 server response sites (POST L334, GET '/' L395 `populate('key_results.owner_id')`, PUT L628) serve from embedded → naive macro drop returns `key_results:[]` on every new objective → silent FE break. Memory `feedback_no_destructive_without_greenlight` explicitly required "precursor safety net (FE audit, backfill, **response-shape helper**)" — slice 1a delivers the helper. **Slice 1a (commit `413f03c`)** — NEW [`server/services/ObjectiveKeyResultsView.js`](../../../server/services/ObjectiveKeyResultsView.js) (170 lines): `forObjective({objective, company_id, populateOwner})` + batch `forObjectives({objectives, company_id, populateOwner})` (N+1-safe via single `$in` query). Collection-first via `KeyResult.find({company_id, objective_id, status:≠cancelled})`; embedded-array fallback for pre-Sprint-22 legacy objectives (collection empty → serve `objective.key_results`). FE-shape projection: collection `quarters[0]` → singular `quarter` (matches what FE consumes); owner_id populate opt-in (`populateOwner=true` for GET '/' matches today's `.populate('key_results.owner_id', 'first_name last_name email name')` PP21 contract; `populateOwner=false` for POST/PUT matches today's no-populate). Wired into 3 sites in `routes/objectives.js`: POST response (L325 area) replaces `key_results: objective.key_results`; GET '/' (L390-420) drops `.populate('key_results.owner_id')` and merges KRs via batch helper; PUT response (L660-680) uses `.toJSON()` (NOT `.toObject({virtuals:true})` — first-run crash on `User.virtual('initials')` dereferencing `first_name[0]` when not in the populated `name email role` projection) to preserve Objective virtuals like `sustained_eligible` (consumed by `client-workspace.js:647`) without triggering User virtuals on the populated owner_id. GET '/:id' + POST /bulk are Planner Engine axios proxies — out of slice 1a scope. PUT `/:objectiveId/progress` goes via `objectiveService` (PX-3.6j-1 owned that path). NEW [`scripts/test-sprint25-PX3.6-slice1a-kr-view-helper.js`](../../../scripts/test-sprint25-PX3.6-slice1a-kr-view-helper.js) **51/51 ✓**: static (15) + helper unit (14, canonical + fallback + batch + populate true/false) + POST runtime (8) + GET runtime (9) + PUT runtime (5). **Slice 1b (commit `9175cd5`)** — drop embedded dual-write at POST + PUT. POST: `processedKeyResults` identifier retired → replaced by `krSpecs` that generates `_id` upfront via `new mongoose.Types.ObjectId()`; Objective constructor uses `key_results: []` (embedded stays empty); `KeyResult.insertMany` is the SOLE write target; failure non-blocking. PUT: `key_results` removed from `allowedFields` (no blanket assignment can mutate embedded); `objective.key_results = updates.key_results.map(...)` REMOVED; `canonicalKRs` built from PUT input WITHOUT mutating embedded; field-preservation lookups (existing KR's `owner_id`/`quarter`/`due_date` when FE omits) now source from `KeyResult.find(...).sort({created_at:1}).lean()` (collection) instead of `objective.key_results.id(kr._id)` (embedded) — preserves PP29 `_id` preservation contract uniformly across old + new objectives; `embeddedIds` Set retired → `canonicalIds`; new KRs (no `_id` from FE) get fresh `new mongoose.Types.ObjectId()` upfront; bulkWrite (upsert + soft-cancel) sources from `canonicalKRs`. `mongoose` require added at top of file. NEW [`scripts/test-sprint25-PX3.6-slice1b-drop-embedded-dual-write.js`](../../../scripts/test-sprint25-PX3.6-slice1b-drop-embedded-dual-write.js) **44/44 ✓**: [1] deleted-idiom static (9 — `processedKeyResults` retired, `key_results:[]` in constructor, no `objective.key_results = ...` mutation in PUT, `embeddedIds` retired, `key_results` removed from PUT allowedFields, etc.) + [2] new-contract markers (7) + [3] POST runtime (8 — persisted embedded === `[]`, 2 collection rows, _id matches response) + [4] PUT runtime (12 — 3 scenarios edit/add/remove; embedded stays `[]` in all; owner_id preserved via collection lookup) + [5] read-side parity (5). **Updated [`scripts/test-sprint25-PX3.6a-objectives-write-migration.js`](../../../scripts/test-sprint25-PX3.6a-objectives-write-migration.js) 32/32 ✓** — the HYBRID dual-write assertions are inverted to assert the slice 1b contract (embedded stays empty after PUT; collection is sole write target). **Updated [`scripts/test-sprint25-PX3.6i-kr-id-passthrough.js`](../../../scripts/test-sprint25-PX3.6i-kr-id-passthrough.js) 30/30 ✓** — PX-3.6i's "embedded subdoc _id ↔ collection _id alignment" contract is SUPERSEDED by slice 1b: single canonical `_id` per KR from creation onward (generated upfront); test now asserts the SUPERSESSION (legacy idioms gone) + response/collection _id parity which IS the canonical surface FE consumes. Full PX-3.6 regression: **14 suites all green** at **391 assertions**, 0 failed. **Why-first audit (5 Qs at slice 1a; 4 Qs at slice 1b)**: 1a-Q1 (helper vs inline at each route) — 7+ response sites need this; DRY + testable. 1a-Q2 (collection-first + fallback) — PX-3.7 backfill hasn't run yet; fallback bridges pre-Sprint-22 legacy. 1a-Q3 (quarters→quarter projection) — FE reads singular. 1a-Q4 (`.toJSON()` vs `.toObject({virtuals:true})`) — caught at first-run test failure when User `initials` virtual crashed on missing `first_name`/`last_name`. 1a-Q5 (split slice 1a from 1b) — 2-commit rollback granularity. 1b-Q1 (drop write before PX-3.7 backfills) — slice 1b doesn't touch existing data, only changes what NEW writes do; helper fallback still serves legacy. 1b-Q2 (collection-source PUT lookups) — embedded is `[]` for new objectives; reading lookup from embedded would lose existing field values. 1b-Q3 (upfront _id generation) — Mongoose only generates subdoc _ids on non-empty embedded; with `key_results:[]` no generation happens; `new mongoose.Types.ObjectId()` is the canonical factory. 1b-Q4 (toJSON re-validation) — same rationale as 1a-Q4, re-validated against slice 1b changes. **Memory rules honored**: per `feedback_no_destructive_without_greenlight`, surfaced the FE-audit blocker BEFORE first edit, requested green-light on revised slice plan (helper-first instead of naive drop), then again before slice 1b ship. Per `Why → What → How → When` framework, user explicitly invoked the reflection pause mid-session ("lets reflect and ask, why are we doing this, what are we doing, how are we doing, the most important timing"); the reflection yielded the consolidation from 4-session plan → 2-session bundle (slice 1a + 1b in one session, 2 commits; slices 2 + 3 next session). Per `Test fixture validation — read schema BEFORE seeding`, read User + KeyResult + Objective schemas before writing krSpecs payload (first-run pass on the krSpecs/canonicalKRs code edits; one regex-window miss caught at first test run). Per `Stay grounded — minimal change`, did NOT rewrite the bulkWrite delta logic — only retargeted its source from `objective.key_results` (embedded) to `canonicalKRs` (in-memory); PP29 `_id` preservation logic unchanged; soft-cancel diff unchanged. **Quality reflection: 9/10**. Strong: caught the FE-blocker mid-flight (per safety memory) before edits — the response helper is the precursor safety net the memory required; per-commit rollback granularity (1a additive / 1b destructive) means a future bug in either layer can be reverted independently; PUT field-preservation lookup migrated from embedded to collection uniformly across old + new objectives (uniform contract is the better fix than dual-source); `.toJSON()` over `.toObject({virtuals:true})` is the schema-respecting choice that doesn't break User virtuals; PX-3.6i contract reframed as "satisfied by construction" rather than left as stale alignment text. Negative: first-run PUT crash on `.toObject({virtuals:true})` should have been anticipated from reading User schema BEFORE the edit (User has no toJSON config + dangerous virtual); 4 regex iterations on slice 1a + slice 1b static checks (window-size + identifier-uniqueness tuning); 3 test-logic errors in slice 1b's runtime scenarios (KR-active count after Scenario C, GET list count) caught by self-test before commit. **Audit IDs**: PX-3.6 macro `A20260506-09` (parent PX-1.11) — slice 1 of 3 closes the `routes/objectives.js` row. PX-3.6 + PX-3.18 prereqs ungated for the remaining 2 slices. **Next session**: `/coding` Day 12 — recommend **PX-3.6 macro slice 2** (`routes/ai-okr.js` 5 write sites — mechanical mirror of slice 1b's pattern: upfront `_id` via `new mongoose.Types.ObjectId()`, `key_results:[]` in constructors, `KeyResult.insertMany` from `krSpecs`-equivalent). After slice 2 ✓, slice 3 (`objective-wizard.js` finalize + `services/objectiveService.js:464` `updateProgress`). After all 3 slices ✓, PX-3.6 macro seals → PX-3.7 backfill → PX-3.18 schema-field drop. Phase 5 sprint-close deliverables (PX-5.1–5.4) still pending — final 4 firing tasks before sprint close.

**Day 9 — PX-3.6h weekly-goals.js HYBRID → MIGRATED canary lift shipped 2026-05-10 (Session #216)**: First **MIGRATED** consumer route in the PX-3.6a-i sweep — closes the consumer-migration phase end-to-end. Scope: drop the Sprint 23 #191c embedded `Objective.key_results[]` fallback in the cross-tenant 404 boundary of `GET /api/weekly-goals/:keyResultId`. Pre-#216 the precheck queried `KeyResult.findOne({_id, company_id})` first and, on miss, fell back to `Objective.findOne({company_id, 'key_results._id': keyResultId})` to cover pre-#190 objectives that only existed embedded. Post-#216 the standalone `KeyResult` query is the SOLE gate; on miss the route 404s straight. **Source change** in [`weekly-goals.js:1-25`](../../../server/routes/weekly-goals.js#L1) (header banner now carries the PX-3.6h MIGRATED rationale + canary positioning + PX-3.6 macro/PX-3.7 backfill/PX-3.18 sequence) and [`weekly-goals.js:184-201`](../../../server/routes/weekly-goals.js#L184) (precheck collapsed from 25 lines to 14 — comment + 5-line `KeyResult.findOne` + 3-line miss-path 404). The `const Objective = require('../models/Objective')` import (line 23 pre-#216) is also dropped — it was solely supporting the retired fallback; `Goal.populate('objective_id', 'title category')` at the legacy union-read path continues to work via mongoose's global model registration (no direct require needed). The Sprint 23 #191 union-read contract (`WeeklyGoal.find` + legacy `Goal{time_period:'WEEKLY'}.find`, projected via `projectNewWeeklyGoal`/`projectLegacyGoal` with `_source: 'new' | 'legacy'` tags) is preserved byte-identical — PX-3.6h only touches the precheck. POST/PUT/DELETE write surface untouched. New regression test [`scripts/test-sprint25-PX3.6h-weekly-goals-kr-read-migration.js`](../../../scripts/test-sprint25-PX3.6h-weekly-goals-kr-read-migration.js) **22/22 ✓** in 6 sections: [1] embedded read is gone (5 — no `'key_results._id'` literal, no `Objective.findOne(`, no Objective `require()`, no `objWithEmbedded`/`krFound` survivors); [2] collection-only precheck is the sole gate (3 — KeyResult import + `KeyResult.findOne({_id, company_id})` shape + 404 miss-path); [3] PX-3.6h lineage tags present (4 — ≥2 occurrences across header + handler, PX-1.11 HYBRID #1 → MIGRATED citation, canary-positioning citation, PX-3.7 backfill citation); [4] Sprint 23 #191 union-read contract preserved (4 — WeeklyGoal find + legacy Goal find + `_source` tags + frontend backward-compat `goals` alias); [5] POST/PUT/DELETE write surface unchanged (4 — POST tenant-scope + WeeklyGoal.create payload + PUT tenant-scope + DELETE soft-delete via `status:cancelled`); [6] parse-load smoke (2 — exports as Express router with `.stack`, explicit `process.exit(0)` because router require boots LLMGateway + AIContextService scaffolding via `aiOKRService` import). PX-1.11 verification matrix flipped: `routes/weekly-goals.js` HYBRID → ✅ **MIGRATED** (Day 9 #216) — first MIGRATED consumer route in the sprint, counts 0+8+2 → **0+7+1+2 (NOT-MIGRATED + HYBRID + MIGRATED + write-MIGRATED)**. Full PX-3.6 drift-lock suite regression-green: 9 suites · PX-3.6a 47/47 + PX-3.6b 54/54 + PX-3.6c 21/21 + PX-3.6d 26/26 + PX-3.6e 30/30 + PX-3.6f 14/14 + PX-3.6g 20/20 + PX-3.6h 22/22 + PX-3.6i 32/32 = **266 PX-3.6 assertions, 0 failed**. **Why-first audit (Q1 canary scope vs co-migrating PX-3.6a-g · Q2 data-readiness for legacy embedded-only KRs · Q3 keep or drop the `Objective` require · Q4 test depth: drift-lock vs runtime)**: Q1 — verified PX-3.6h spec literally says "drop embedded fallback" and is sequenced BEFORE PX-3.6 macro and PX-3.7 backfill; the master plan deliberately positions weekly-goals.js as the canary because its embedded fallback is in a *precheck* (cross-tenant 404 boundary), not a data-fetch path — failure mode for any pre-Sprint-22 embedded-only KR is a clean 404 not data corruption. Co-migrating all 7 HYBRID routes simultaneously was rejected: the master plan explicitly schedules that as the PX-3.6 macro task, gated on PX-3.6a-h ALL ✓ + PX-1.11 GREEN. Q2 — pre-#216 dual-writes (PX-3.6a + PX-3.6b + PX-3.6c) ensure every write path now creates both standalone + embedded rows, so any KR created post-Sprint-22 is collection-resident; only pre-Sprint-22 legacy data can be embedded-only. PX-3.7 (`scripts/db/migrate-legacy-weekly-to-new.js`) is the planned backfill, scheduled BEFORE PX-3.18 (schema-field drop). Accepted that pre-Sprint-22 embedded-only KRs WILL 404 on this single route between PX-3.6h ship and PX-3.7 backfill — that's the intentional canary signal: it surfaces data-readiness gaps before the rest of the routes follow suit via PX-3.6 macro. Q3 — grep confirmed `Objective` was used only at the dropped fallback site (no other `Objective.` reference in the file); the populate('objective_id', ...) call at the legacy union-read works via mongoose's global model registry (the model is loaded at server startup elsewhere). Dropping the require keeps the file's import surface honest. Q4 — chose static drift-lock (mirroring PX-3.6c rationale) because: the migration delta is *deletion* (no behavior to assert at runtime — only absence of behavior), and the standalone-KR-collection-only contract is already runtime-proven by every PX-3.6a-c HYBRID test (creating a KR via PUT/POST + asserting collection has the row). The 22 static assertions exhaustively pin the *absence* of the fallback (5 negative checks in section [1]) plus the *presence* of the collection-only gate plus the preservation of the surrounding Sprint 23 #191 union-read contract — that's the actual surface area the next reader needs protected. **Memory rules honored**: greppped `Objective` references in `weekly-goals.js` BEFORE the edit pass to verify the import was dropable (per `Test fixture validation — read schema BEFORE seeding` adapted to imports — caught the same lesson PX-3.6b's missing-import bug forced last session). Per `Stay grounded — minimal change` — did NOT rewrite the union read or the projector functions, only collapsed the precheck and dropped one require; PX-3.6h is intentionally surgical. Per `Why → What → How → When` — the Why ("first MIGRATED canary surfaces data-readiness before PX-3.6 macro spreads the fallback drop to 7 more routes") drove the choice to drop the fallback completely rather than merely tagging the existing HYBRID code with a lineage comment (that would have been the PX-3.6c-style formality, but the spec here is genuinely a behavior change). Per `feedback_audit_governance` — every PX-3.6 task has a drift-lock test under `scripts/test-sprint25-PX3.6X-*.js`; PX-3.6h follows the convention with a static + parse-load test, explicit `process.exit(0)` to avoid LLMGateway boot hangs (lesson learned from #215). Frontend planning page is the sole consumer of this endpoint per `client/pages/planning-v2.js` and reads `goals` (frontend backward-compat alias preserved by the projector) — no FE change needed. **Quality reflection: 9/10**. Strong: surgical scope kept the diff to ~30 lines + 1 import drop + 1 new test file; 5 negative-shape assertions in section [1] exhaustively pin the deletion against re-introduction (you can't accidentally re-add `'key_results._id'`, the `Objective` import, or the `objWithEmbedded`/`krFound` identifiers without breaking the test); preserved the Sprint 23 #191 union-read contract via 4 explicit positive assertions in section [4] so future migrations can't silently regress the legacy-data path; lineage chain (`Sprint 25 PX-3.6h` references PX-1.11 + PX-3.6 macro + PX-3.7 backfill + PX-3.18 schema drop) makes the next reader's audit trail complete; first-run test pass on regex (no iteration) because schema/file shape was greppped upfront. Negative: didn't think to verify the FE caller path (`planning-v2.js`) until reflection — should have been part of the Why-first sweep before code change; per memory `Two-app model`, both Consultant and Client apps may hit this endpoint and a 404 in one app could surface differently than the other. Mitigation: 404 message body is identical to pre-#216 ("Key result not found") so error-display surfaces in both apps stay byte-identical; no FE change needed and no consultant-scoped variant exists. **Audit IDs**: PX-1.11 macro `A20260506-09` already ✓ Plan + ✓ Code from #209 — PX-3.6h closes the last sub-tracker row in `SPRINT_X_VERIFICATION.md` consumer migration table (sub-tracker now reads "all 8 routes ≥ HYBRID, 1 MIGRATED — PX-3.6 macro unblocked"). **Next session**: `/coding` Day 10 — recommend **PX-3.6 macro** (drop embedded write at all POST routes simultaneously: `objectives.js`, `ai-okr.js` ×5 sites, `objective-wizard.js` finalize) AND drop the 7 HYBRID embedded-read fallbacks (`consultant.js`, `cascade.js`, `planning.js` ×3 sites, `moves.js`, plus the read-side fallback in `objectives.js` if any). After PX-3.6 ✓, PX-3.7 (`scripts/db/migrate-legacy-weekly-to-new.js` idempotent backfill with `--dry-run`) + PX-3.8 (block legacy POST/PUT/DELETE with 410 Gone) + PX-3.9-12 (dry-run on pre-prod, regression suite, rollback doc, test-data execution) + PX-3.18 (drop `Objective.key_results[]` schema field entirely). Phase 5 sprint-close deliverables (PX-5.1–5.4 mandatory) still pending — they're the final 4 firing tasks before sprint close.

**Day 8 — PX-3.6c objective-wizard.js HYBRID formalization shipped 2026-05-09 (Session #215)**: Smallest task in the PX-3.6 series — purely a scope-completion / drift-lock task. Why-first audit established: the actual code migration was already done by Sprint 23 Epic E (D-A-1, D-E-7 — added the dual-write at [`objective-wizard.js:736`](../../../server/routes/objective-wizard.js#L736) `KeyResult.insertMany(krDocs)`) and refined by PX-3.6i Day 5 #212 (added `_id: kr._id` pass-through to align collection `_id` with embedded subdoc `_id`). The PX-1.11 verification table marking `objective-wizard.js` as NOT-MIGRATED was stale — the dual-write contract had been in place for two prior sessions. PX-3.6c's actual scope: (1) light up the `Sprint 25 PX-3.6c` lineage tag in the existing dual-write comment so future readers see the explicit Sprint 25 ownership alongside the older Sprint 23 + PX-3.6i citations; (2) ship a static drift-lock test that pins the dual-write contract against future regression — gate on embedded array length, `_id: kr._id` pass-through, year mirrors `targetYear` (D-A-4 invariant), `baseline_value: 0`, try/catch + non-blocking warn, dual-write block sits AFTER `await newObjective.save()`; (3) flip the verification matrix. **Source change** in [`objective-wizard.js:732-736`](../../../server/routes/objective-wizard.js#L732): comment block now carries Sprint 23 (D-A-1, D-E-7) + Sprint 25 PX-3.6c (formal lineage + HYBRID rationale) + Sprint 25 PX-3.6i (`_id` pass-through) — 3-sprint citation chain documents the migration's history end-to-end. **No behavior change**: dual-write code is byte-identical to pre-#215. New regression test [`scripts/test-sprint25-PX3.6c-objective-wizard-write-migration.js`](../../../scripts/test-sprint25-PX3.6c-objective-wizard-write-migration.js) **21/21 ✓** in 3 sections: file-level invariants (4 — KeyResult import + PX-3.6c lineage tag + Sprint 23 Epic E lineage retained + PX-3.6i `_id` pass-through citation retained); finalize dual-write block invariants (15 — gating, `newObjective.key_results.map`, `_id: kr._id`, companyId scope, `objective_id: newObjective._id` FK, `year: targetYear` with D-A-4 citation, `baseline_value: 0`, quarter→quarters array, `ai_generated: true`, `KeyResult.insertMany(krDocs)`, try/catch wrapping, non-blocking warn, embedded write retained, `await newObjective.save()` precedes block); parse-load smoke (2 — exports as Express router with `.stack`). **Note on test exit**: wizard router require boots LLMGateway scaffolding which keeps the event loop alive; added explicit `process.exit(0)` on success to prevent hangs in regression sweeps (caught when sweep loop hung mid-test on first run; PX-3.6b test doesn't have this issue because ai-okr.js doesn't pull in the same scaffolding). PX-1.11 verification doc updated: `objective-wizard.js` NOT-MIGRATED → HYBRID ✓ #215; PX-3.6c row flipped NOT-STARTED → ✅ HYBRID #215; counts 1+7+2 → **0+8+2 — first time this sprint with zero NOT-MIGRATED consumer routes**. Verdict line updated: only PX-3.6h (`weekly-goals.js` HYBRID → MIGRATED) gates the PX-3.6 macro + PX-3.18 schema-field drop. Regression sweep all green: 11 S25 suites · #209 36/36 + PX-2.8 55/55 + PX-2.9 32/32 + PX-3.6a 47/47 + PX-3.6b 54/54 + PX-3.6c 21/21 + PX-3.6d 26/26 + PX-3.6e 30/30 + PX-3.6f 14/14 + PX-3.6g 20/20 + PX-3.6i 32/32 = **367 S25 assertions, 0 failed**. **Why-first audit (Q1 scope of PX-3.6c · Q2 test depth · Q3 commit boundary with Day 7)**: Q1 — verified by re-reading `objective-wizard.js:30` (KeyResult import already present) + `:741-758` (full dual-write already present) + Day 5 #212 narrative (PX-3.6i refined the wizard finalize site, citing line 741); the migration genuinely was done two sessions ago, just never formally flipped. Choosing to ship a 21-assertion drift-lock test rather than just flipping the doc — per memory rule `feedback_audit_governance` (every audit assigns IDs + tracks Plan→Code→Tests). Q2 — static-only test mirrors PX-3.6f + PX-3.6b rationale: dual-write contract + `_id` alignment already runtime-proven by PX-3.6a (6 scenarios) + PX-3.6i; the wizard runtime path requires session-state scaffolding (initialize-session → refine-objective → generate-krs → regenerate-kr → finalize) that exercises wizard infrastructure not the migration delta. Per memory `Stay grounded — minimal change` — smallest amendment: 5 lines of comment edit + 21-assertion test. Q3 — surfaced to user that Day 7 PX-3.6b is uncommitted (4 files modified, new test file untracked); recommended either two feature commits (`feat(sprint25 #214): PX-3.6b` + `feat(sprint25 #215): PX-3.6c`, matching Day 6's per-task pattern) OR one bundled commit, deferring to user preference at /close. **Memory rules honored**: greppped objective-wizard.js BEFORE editing to verify what was already there (caught that PX-3.6c was a formality not a code migration — saved an entire migration's worth of redundant work); per `Stay grounded — minimal change`, did NOT rewrite the dual-write block, just added the lineage-tag comment alongside existing Sprint 23 + PX-3.6i citations. Per `Why → What → How → When` — the Why ("ensure verification matrix is accurate so PX-3.6 macro + PX-3.18 know they're safe") drove a much smaller scope than naively interpreting "PX-3.6c = migrate file" would have suggested. Test required explicit `process.exit(0)` on success (caught at sweep run, not first-run); should have anticipated wizard's LLMGateway boot keeping the event loop alive (same risk every wizard-routed test would face). **Quality reflection: 9/10**. Strong: refused to do redundant work after grep showed the migration was already shipped; chose static drift-lock over runtime; 3-sprint citation chain in the comment (Sprint 23 Epic E + PX-3.6c + PX-3.6i) makes lineage explicit for the next reader; flagged Day 7 commit boundary to user instead of silently bundling. Negative: missed the wizard's LLM-boot event-loop hang on first sweep run — should have anticipated it from the parse-load smoke design (the same `require()` pattern would hang on regression loops); 2 stop-and-restart cycles before adding `process.exit(0)`. **Audit IDs**: PX-1.11 macro `A20260506-09` already ✓ Plan + ✓ Code from #209; sub-tracker is `SPRINT_X_VERIFICATION.md` consumer migration table — flipped to all-HYBRID-or-better this session. **Next session**: `/coding` Day 9 — recommend **PX-3.6h** (`weekly-goals.js` HYBRID → MIGRATED — drop the embedded fallback at the goal-link site). After PX-3.6h ✓, PX-3.6 macro (drop embedded write at all POST sites) and PX-3.18 (drop `Objective.key_results[]` schema field) become safe. Phase 5 sprint-close deliverables (PX-5.1–5.4) still pending — could fold into Day 9 or its own Day 10 session.

**Day 7 — PX-3.6b ai-okr.js write-side migration shipped 2026-05-09 (Session #214)**: Highest-risk migration in the PX-3.6 suite — `ai-okr.js` owns the SOLE cascade-acceptance KR write path. All 5 KR-creation sites now HYBRID dual-write to the standalone `KeyResult` collection after `objective.save()`, mirroring PX-3.6a's POST-side pattern verbatim with PX-3.6i `_id` pass-through (`_id: kr._id` sourced from post-save embedded subdocs). Sites migrated: **(1)** `POST /api/ai-okr/approve` (legacy approve via AIOKRSuggestion, [`ai-okr.js:670`](../../../server/routes/ai-okr.js#L670)); **(2)** `POST /api/ai-okr/approve-draft` (active approve path, [`ai-okr.js:1062`](../../../server/routes/ai-okr.js#L1062)); **(3)** `POST /api/ai-okr/generate-from-company` cascade-acceptance writer (the SOLE cascade-acceptance path; comment block explicitly cites this, [`ai-okr.js:2385`](../../../server/routes/ai-okr.js#L2385)); **(4)** `POST /api/ai-okr/generate-single-objective` AI-disabled template fallback ([`ai-okr.js:2635`](../../../server/routes/ai-okr.js#L2635)); **(5)** `POST /api/ai-okr/generate-single-objective` AI-enabled main ([`ai-okr.js:3186`](../../../server/routes/ai-okr.js#L3186)). Each block is identical: gates on `if (objective.key_results.length)`, sources `_id` from post-save embedded subdocs, builds `krDocs` with 12 fields (`_id`, `company_id`, `objective_id`, `title`, `description`, `metric_type`, `target_value`, `baseline_value: 0`, `current_value`, `unit`, `quarters: kr.quarter ? [kr.quarter] : []`, `year: objective.target_year`, `owner_id`, `created_by: userId`, `ai_generated: true`), wraps `KeyResult.insertMany(krDocs)` in `try/catch (dualWriteErr)`, logs `[AI OKR Routes] KeyResult dual-write failed (non-blocking)` warn on failure. **Critical: KeyResult import was missing from ai-okr.js** — added `const KeyResult = require('../models/KeyResult');` at imports block; pre-merge would have crashed all 5 routes with `ReferenceError`. Caught at first test run (static check `ai-okr.js imports KeyResult model` failed). New regression test [`scripts/test-sprint25-PX3.6b-ai-okr-write-migration.js`](../../../scripts/test-sprint25-PX3.6b-ai-okr-write-migration.js) **54/54 ✓** in 4 sections: file-level invariants (4 — KeyResult import + 5 PX-3.6b blocks + 5 insertMany + 5 non-blocking warns); per-site invariants (45 — 9 checks × 5 sites: gating, `_id` pass-through, `target_year` mapping, `objective_id` FK, `userId` as `created_by`, `baseline_value: 0`, `quarter`→`quarters` array, try/catch wrapping, `ai_generated: true` + Site 3 SOLE-WRITER comment); HYBRID-posture proof (3 — embedded-write constructors retained, every block sits AFTER an `await objective.save()` — verified by walking 1000-char preceding window for each block); parse-load smoke (2 — exports as Express router function with `.stack`). PX-1.11 verification doc updated: `ai-okr.js` NOT-MIGRATED → HYBRID ✓ #214 (PX-3.6b); PX-3.6b table row flipped NOT-STARTED → ✅ HYBRID #214; counts 2+6+2 → **1+7+2** (only `objective-wizard.js` NOT-MIGRATED remains, owned by PX-3.6c). Regression sweep all green: 13 suites · S24 #241 73/73 + S24 #243 197/197 + S24 #244 75/75 + S25 #209 36/36 + S25 #210 PX-2.8 55/55 + PX-2.9 32/32 + PX-3.6g 20/20 + S25 #211 PX-3.6e 30/30 + S25 #212 PX-3.6i 32/32 + PX-3.6d 26/26 + PX-3.6f 14/14 + S25 #213 PX-3.6a 47/47 + S25 #214 PX-3.6b 54/54 = **691 assertions, 0 failed**. **Why-first audit (Q1 bundle vs split · Q2 HYBRID vs collection-only · Q3 runtime vs static test · Q4 broken-site treatment)**: Q1 bundled all 5 sites in one session — they're mechanical mirrors of PX-3.6a; splitting buys nothing and risks half-shipping the SOLE cascade-acceptance writer. Q2 HYBRID mandatory — same constraint as every prior PX-3.6 migration: PX-3.6 macro hasn't dropped consumer fallbacks yet, so collection-only writes at this site would create a sequencing inconsistency. Q3 chose static-only test (mirroring PX-3.6f's rationale) — dual-write contract already runtime-proven by PX-3.6a's 6 scenarios on the mechanically-identical POST `/api/objectives` site, AND `_id` alignment already runtime-proven by PX-3.6i. The 5 ai-okr.js sites also have pre-existing constructor-level validation gaps (sites 1, 2, 4 don't pass `end_date`/`target_year`, would fail save() today before my dual-write block fires) — running them at runtime would require fixing pre-existing bugs out of PX-3.6b scope; sites 3 and 5 are LLM-dependent and would mostly exercise LLMGateway scaffolding, not the migration delta. Q4 explicitly chose NOT to fix the pre-existing `end_date`/`target_year` gaps — this is a migration task, not a bug-fix task. My dual-write block sits AFTER `objective.save()` so on broken paths it's dead code today (same posture embedded write was before — no regression introduced). Captured pre-existing gaps as candidate refinement-track item. **Memory rules honored**: read Objective + KeyResult schemas BEFORE writing krDocs payload (caught `baseline_value: 0` and `year` required upfront — first-run pass on the per-site code edits). Caller audit before code: traced `userId` and `company_id` scope at all 5 sites via `grep -n "router\.\|userId\s*=\|company_id\s*="` → confirmed every site has both in scope; spotted that site 1's Objective constructor lacks `target_year`/`end_date` BEFORE choosing test approach (informed Q3/Q4). KeyResult import gap caught by the test's first run, not at code-edit time — that's the regret of the day; should have grepped `require.*KeyResult` in ai-okr.js BEFORE the first edit (not after). Tightened the test's PX-3.6b marker to avoid false-matching the import comment (`// PX-3.6b dual-write` instead of `// Sprint 25 PX-3.6b — HYBRID dual-write target`) after seeing 6 blocks detected on first run. **Quality reflection: 8/10** (down from 9 because of the missing-import miss). Strong: 5-site bundle shipped clean with identical pattern; cascade-acceptance site explicitly cited SOLE WRITER in comment to flag downstream readers; static test design choice rationalized via 4-question Why-first audit + mirrored to PX-3.6f's proven approach; 54-assertion test caught 3 of my own bugs (block-window size + try-block lazy range + missing import) before code shipped — test-as-design-pressure worked. Negative: missed the KeyResult import on first edit pass — should have been my opening grep before any Edit call (per memory `Test fixture validation — read schema BEFORE seeding`, applied to imports too); 3 regex tuning iterations on the test (block window 1200→2200, try-block lazy 800→1500, marker uniqueness) — could have computed actual block sizes upfront before writing the regex (`grep -B1 -A30 "PX-3.6b" | wc -c`). **Audit IDs**: PX-1.11 macro `A20260506-09` already ✓ Plan + ✓ Code from #209; sub-tracker is the `SPRINT_X_VERIFICATION.md` consumer migration table — flipped this session. **Next session**: `/coding` Day 8 — recommend **PX-3.6c** (`objective-wizard.js` finalize, only NOT-MIGRATED file left); mechanical mirror of PX-3.6a/b. After PX-3.6c lands, only PX-3.6h (`weekly-goals.js` HYBRID → MIGRATED) remains in the consumer-migration phase before the PX-3.6 macro (drop embedded write at all POST sites) + PX-3.18 (drop `Objective.key_results[]` schema field) become safe. Phase 5 sprint-close deliverables (PX-5.1–5.4) still pending.

**Day 6 — PX-3.6a write-side migration shipped 2026-05-09 (Session #213)**: First write-side migration of the PX-3.6a-c trio, scoped per Day-5 close to `objectives.js` (route + Objective model methods) only — PX-3.6b (`ai-okr.js`) explicitly deferred to Day 7 because it owns the SOLE cascade-acceptance KR write path across 5 sites. **PUT `/api/objectives/:id`** now does a post-save bulkWrite KR collection sync, scoped on `if (updates.key_results && Array.isArray(updates.key_results))` — three deltas handled in a single `bulkWrite(ops, {ordered: false})`: (a) existing KR edited → upsert by embedded subdoc `_id` ($set fields, $setOnInsert never fires for existing rows); (b) new KR added → upsert with `$setOnInsert: {_id, company_id, objective_id, year, baseline_value:0, created_by}` (PX-3.6i alignment preserved on PUT — proven by Scenario B); (c) KR removed via PUT → soft-cancel only (status='cancelled', no hard delete) by diffing `objective.key_results._id` Set against active collection KRs and pushing $set ops for the difference. Pre-existing PP29 fix (`_id: kr._id || existingKR?._id` to maintain Goal links) untouched. Sync wrapped in `try/catch` with `logger.warn(...non-blocking...)` — embedded write is the source of truth and must not roll back if collection sync fails (matches the POST dual-write pattern at L231). **`Objective.prototype.addKeyResult`** converted from sync `function` returning `this.save()` to `async function`; after the embedded push + save, calls `KeyResult.create({_id: newKR._id, …})` sourcing `_id` from the post-save subdoc to preserve PX-3.6i alignment. **`Objective.prototype.updateKeyResultProgress`** also converted to `async`; after embedded mutation + save, calls `KeyResult.findByIdAndUpdate(keyResultId, {$set: {current_value, status}})` — only mirrors the two fields the method actually touches (avoids overwriting collection-only fields like `baseline_value`). Both methods use `mongoose.model('KeyResult')` rather than `require('./KeyResult')` to avoid the circular import that Objective.js → KeyResult would create. Caller audit before coding: `addKeyResult` / `updateKeyResultProgress` have **zero callers in `server/`** today; migration is forward-compat insurance, not a hot path. New regression test [`scripts/test-sprint25-PX3.6a-objectives-write-migration.js`](../../../scripts/test-sprint25-PX3.6a-objectives-write-migration.js) **47/47 ✓** in 7 sections: static (12 — PX-3.6a citations + bulkWrite present + upsert by `kr._id` + $setOnInsert.year + soft-cancel via $set status=cancelled + non-blocking warn + `_id: newKR._id` pass-through in addKeyResult + findByIdAndUpdate in updateKeyResultProgress + both methods async + bare-`return this.save()` removed); Express boot; **Scenario A** (PUT edits existing KR — collection title/target_value/current_value updated, `_id` preserved per PP29, sibling KR untouched); **Scenario B** (PUT adds new KR — embedded gets fresh `_id`, collection upsert creates row with same `_id`, count goes 2→3); **Scenario C** (PUT removes KR — collection row still exists but `status='cancelled'`, active count drops 2→1); **Scenario D** (PUT with title-only body — collection KR count + titles unchanged, sync correctly skipped); **Scenario E** (`addKeyResult` — embedded + collection both have new KR with same `_id`, year mirrors `objective.target_year`); **Scenario F** (`updateKeyResultProgress` — both stores reflect `current_value=60` + `status=in_progress`, then `current_value=100` + `status=completed`). PX-1.11 verification doc updated: `objectives.js` NOT-MIGRATED → HYBRID ✓ #213; counts 3+5+2 → **2+6+2** (only 2 NOT-MIGRATED remain — `ai-okr.js` and `objective-wizard.js`, both write-side, owned by PX-3.6b/c). Regression sweep all green: 12 suites · S24 #243 197/197 + S22a #184e 36/36 + S24 #241 73/73 + S25 #209 36/36 + S25 #210 PX-2.8 55/55 + PX-2.9 32/32 + PX-3.6g 20/20 + S25 #211 PX-3.6e 30/30 + S25 #212 PX-3.6i 32/32 + PX-3.6d 26/26 + PX-3.6f 14/14 + S25 #213 PX-3.6a 47/47 = **598 assertions, 0 failed**. **Why-first audit (Q1 bundle/split scope · Q2 dual-write vs collection-only · Q3 instance methods migrate vs deprecate · Q4 PX-3.6b deferral)**: Q1 bundled route + model methods because the audit listed them together and leaving methods un-migrated would create a silent escape hatch for future callers. Q2 chose dual-write (HYBRID) because PX-3.6h hasn't dropped consumers' embedded fallback yet — collection-only at PX-3.6a would create a sequencing inconsistency with the rest of the sprint. Q3 chose migrate-not-deprecate after caller audit found zero callers in `server/`: deprecating without grepping every test/script/external caller is unnecessary risk; migrating is 8-line dual-write. Q4 confirmed defer — `ai-okr.js` writes the SOLE cascade-acceptance path at 5 sites (`ai-okr.js:640/1015/2299/2537/3037` per #211 audit); breaking it would zero out cascade KRs entirely. Day 6 stayed scoped to `objectives.js` + `Objective.js`. **Memory rules honored**: read Objective embedded KR subdoc + KeyResult schemas before writing krDocs $setOnInsert payload (caught `baseline_value:0` requirement upfront — first-run pass). Caller audit on instance methods done BEFORE writing the migration (per `Why → What → How → When` framework). `mongoose.model('KeyResult')` deferred-resolve pattern explicitly chosen over `require('./KeyResult')` to avoid Objective↔KeyResult circular import (per `Carry skepticism toward "build new" framings` — verified the circular risk by tracing `KeyResult.js` imports). Tightened the PUT sync to fire only when `updates.key_results` was actually present (Scenario D directly proves this) — initial draft fired on every PUT, would have caused unnecessary DB load on title-only edits; caught + fixed before test write. **Quality reflection: 9/10**. Strong: PUT scenarios A/B/C/D directly map to the three CRUD deltas + the negative case; PX-3.6i alignment preserved on PUT (Scenario B's `_id` equality assertion is the linchpin); Scenario F doubles as a regression for `calculateProgress()` recursion (`updateKeyResultProgress` calls `this.calculateProgress()` which iterates `this.key_results`); 47-assertion test wrote first-run pass with no fixture iterations because schema reads were done upfront (memory rule clean this time, breaking the streak from #212). Negative: noticed mid-write that the initial PUT sync didn't gate on `updates.key_results` (would fire on every PUT) — caught before test write so no rework cost, but should have been spotted in the design pass before the first edit. **Audit IDs**: PX-1.11 macro `A20260506-09` already ✓ Plan + ✓ Code from #209; sub-tracker is `SPRINT_X_VERIFICATION.md` consumer migration table. **Next session**: `/coding` Day 7 — recommend **PX-3.6b** (`ai-okr.js` 5 cascade-write sites; highest-risk in the migration suite because it's the SOLE writer of cascade-acceptance KRs). PX-3.6c (`objective-wizard.js` finalize) is mechanical mirror of PX-3.6a once PX-3.6b lands. After PX-3.6a/b/c + PX-3.6h, the PX-3.6 macro (drop embedded write at all POST sites) and PX-3.18 (drop schema field) become safe. Phase 5 sprint-close (PX-5.1–5.4) still pending.

**Day 5 — PX-3.6i + PX-3.6d + PX-3.6f shipped 2026-05-09 (Session #212)**: Three-task day clearing the entire PX-3.6d/f-via-PX-3.6i unblock chain set up by Day 4. **PX-3.6i** (KR `_id` pass-through, the prereq): both dual-write sites — POST `/api/objectives` at [`objectives.js:233`](../../../server/routes/objectives.js#L233) and POST `/api/objective-wizard/finalize` at [`objective-wizard.js:741`](../../../server/routes/objective-wizard.js#L741) — switched from mapping over the input `processedKeyResults` / `embeddedKRs` arrays to mapping over the post-save `objective.key_results` / `newObjective.key_results` (Mongoose-assigned subdoc `_id`s) and passing `_id: kr._id` into the `KeyResult.insertMany(krDocs)` payload. Net result: collection KR `_id` === embedded subdoc `_id` for every new dual-write going forward; `Goal.key_result_id` (historically pinned to embedded `_id`) now resolves either store. The third site flagged Day 4 (`key-results.js:85`) is a **collection-only write** — re-audited and confirmed not actually a dual-write — so PX-3.6i scope settled at 2 sites, not 3 (verification doc updated). New regression test [`scripts/test-sprint25-PX3.6i-kr-id-passthrough.js`](../../../scripts/test-sprint25-PX3.6i-kr-id-passthrough.js) **32/32 ✓** in 3 sections: static check on both routes; runtime POST `/api/objectives` with 3 KRs → `Objective.findById` + `KeyResult.find` Set-comparison proves embedded `_id` Set === collection `_id` Set (no drift either way); spot-check field fidelity (title, objective_id, company_id, year) on every KR; Goal-FK simulation by embedded `_id` directly resolves into collection. Wizard runtime path skipped intentionally — the edit is mechanically identical and runtime-proven via the objectives.js path; the wizard scaffolding (session state + draft objective + LLM-linked finalize) would mostly exercise wizard infrastructure, not the `_id` change. **PX-3.6d** (`consultant.js`, mechanical mirror of PX-3.6e): GET `/api/consultant/clients/:id/goals/quarterly` now fetches `KeyResult.find({company_id, objective_id: {$in:[…]}, status:{$ne:'cancelled'}}).lean()` after Goal page slice; builds `krsByObjective` Map keyed by `String(objective_id)`; swaps `g.objective_id.key_results` with collection rows for Objectives that have collection KRs; leaves embedded `key_results[]` populated as HYBRID fallback for Objectives without collection rows (AI-cascade + edited Objectives). FE FK-lookup pattern (`objective_id.key_results.find(k => k._id === goal.key_result_id)`) keeps working unchanged because PX-3.6i now guarantees `_id` alignment. New regression test [`scripts/test-sprint25-PX3.6d-consultant-kr-migration.js`](../../../scripts/test-sprint25-PX3.6d-consultant-kr-migration.js) **26/26 ✓** in 5 sections: static (8 — KR import + KeyResult.find scoped + Map build + swap statement + populate retained + PX-3.6d/3.6i citations); Express boot; Scenario A (collection-only — proves migration: response carries 'COLLECTION-ONLY-KR'); Scenario B (embedded-only — proves HYBRID fallback retains 'EMBEDDED-FALLBACK-KR' + FE FK lookup resolves); Scenario C (PX-3.6i alignment — collection 'FRESH-COLLECTION-VALUE' wins over embedded 'STALE-EMBEDDED-VALUE'; FE FK lookup by Goal.key_result_id resolves into the collection KR). **PX-3.6f** (`planning.js`, three KR-read sites — generate-weekly-plan ~L765, append-weekly-plan-extension ~L1324, generate-tasks ~L1777): each site replaced `objective.key_results.id(key_result_id)` (or `goal.objective_id.key_results.id(goal.key_result_id)` at site 3) with `KeyResult.findOne({_id, company_id, objective_id, status:{$ne:'cancelled'}}).lean()` first, falling back to the embedded subdoc lookup only when the collection has no row. Added `const KeyResult = require('../models/KeyResult')` to the import block. Test [`scripts/test-sprint25-PX3.6f-planning-kr-migration.js`](../../../scripts/test-sprint25-PX3.6f-planning-kr-migration.js) **14/14 ✓** in 2 sections: static (12 — KR import + 3 PX-3.6f comment blocks + 3 KeyResult.findOne lookups + scoped by company_id/objective_id/status + 3 embedded `key_results.id()` fallbacks retained + every fallback preceded by a KeyResult.findOne in the prior 30 lines); parse-load smoke (router exports as a function). Runtime path skipped: planning.js routes pull in `aiPlanningLimiter` + `llmGateway` + `aiContextService` + heavy template-fallback context-building; the migration is a 3-line per-site pattern identical to PX-3.6d's runtime-proven case (alignment guarantee already runtime-proven by PX-3.6i; FE FK lookup pattern already runtime-proven by PX-3.6d). PX-1.11 verification doc updated: `consultant.js` NOT-MIGRATED → HYBRID ✓ #212 (PX-3.6d); `planning.js` NOT-MIGRATED → HYBRID ✓ #212 (PX-3.6f); PX-3.6i flipped to ✅ #212; counts 5+3+2 → **3+5+2** (3 NOT-MIGRATED remain: `objectives.js`, `ai-okr.js`, `objective-wizard.js` — all write-side, owned by PX-3.6a/b/c). Regression sweep all green: 11 suites · S24 #243 197/197 + S22a #184e 36/36 + S24 #241 73/73 + S25 #209 36/36 + S25 #210 PX-2.8 55/55 + PX-2.9 32/32 + PX-3.6g 20/20 + S25 #211 PX-3.6e 30/30 + S25 #212 PX-3.6i 32/32 + PX-3.6d 26/26 + PX-3.6f 14/14 = **551 assertions, 0 failed**. **Why-first audit (Q1 ordering · Q2 wizard runtime · Q3 PX-3.6f static-only · Q4 third dual-write site)**: Q1 confirmed sequencing (i → d → f) — `_id` alignment must ship before consumer migrations or HYBRID Scenario C in d/f tests would fail. Q2 chose static-only on wizard side of PX-3.6i because the wizard path requires session-state scaffolding that mostly exercises wizard infrastructure, not the `_id` change; the edit is mechanically identical to objectives.js POST and runtime-proven there. Q3 chose static-only on PX-3.6f because all 3 sites mirror PX-3.6e/g/d's pattern, all already runtime-proven; planning.js runtime would mostly exercise AI scaffolding. Q4 re-read `key-results.js:85` and confirmed it does NOT dual-write to embedded — it's collection-only, so the Day 4 audit's "3 dual-write sites" was an over-count; PX-3.6i scope is genuinely 2 sites today (and any third that materializes from PX-3.6a/b/c, which would inherit the same pattern). **Memory-rule honored**: Objective + KeyResult + Goal + User schemas all pre-checked before seeding; PX-3.6i fixture first-run pass; PX-3.6d test caught two fixture costs (User.pre('save') consultant `company_id` requirement + Objective.category enum + Goal `created_by`/`due_date` required), all schema-grep iterations not Mongoose-error iterations. **Quality reflection: 9/10**. Strong: shipped a 3-task chain in one session without scope creep; PX-3.6f stayed mechanical because PX-3.6i landed first; runtime/static decisions made on test-cost-vs-novelty grounds (not "more tests = better"); discovered the over-count on PX-3.6i scope by re-reading `key-results.js` rather than blindly forcing a 3-site change; re-used PX-3.6e/g test scaffolding (seedScenario helper, Scenario A/B/C structure) to keep test-author cost low. Negative: PX-3.6d test required two fixture iterations (consultant `company_id` + Goal `created_by`/`due_date`) — should have grepped Goal `required` and User.pre hooks upfront in the same pass as Objective + KeyResult; that's the exact memory rule, partially honored. **Audit IDs**: PX-1.11 macro `A20260506-09` already ✓ Plan + ✓ Code from #209; sub-task moves are tracked via the `routes/*` table in [SPRINT_X_VERIFICATION.md](../../2-TECHNICAL/SPRINT_X_VERIFICATION.md#consumer-migration-status). **Next session**: `/coding` Day 6 — only PX-3.6h (`weekly-goals.js` HYBRID → MIGRATED) and the 3 write-side migrations PX-3.6a/b/c remain in the consumer-migration phase before the PX-3.6 macro (drop embedded write at POST sites) and PX-3.18 (drop schema field) become safe. Recommend **PX-3.6a** (`objectives.js` POST + PUT — primary write site) first; PX-3.6b (ai-okr.js cascade-write) is the highest-risk because it's currently the ONLY path that writes the cascade-acceptance KRs, and dropping its embedded write before the cascade-engine sub-objective fix lands would zero out cascade KRs entirely. Phase 5 sprint-close deliverables (PX-5.1–5.4) still pending.

**Day 4 — PX-3.6e `cascade.js` shipped + PX-3.6d/f resequenced 2026-05-08 (Session #211)**: Second of the PX-3.6a-h consumer migrations. Three embedded `Objective.key_results[]` reads in [`server/routes/cascade.js`](../../../server/routes/cascade.js) at lines 270/281/292 (pre-migration — root + dept + team objectives, all in `GET /api/cascade/hierarchy/:id`) replaced with collection-first `KeyResult.find({company_id, objective_id: {$in: [root, ...depts, ...teams]}, status: {$ne: 'cancelled'}}).lean()` + `krsByObjective` Map + `resolveKRs(obj)` helper that prefers collection KRs, falls back to per-objective embedded `key_results[]` when collection has none for that Objective (HYBRID until PX-3.6 macro drops fallback). Stub `Department` + `Team` models registered in test to satisfy route's `.populate('assigned_departments')` / `.populate('assigned_teams')` calls (no real models needed for KR-resolution coverage). New regression test [`scripts/test-sprint25-PX3.6e-cascade-kr-migration.js`](../../../scripts/test-sprint25-PX3.6e-cascade-kr-migration.js) **30/30 ✓** in 5 sections: static source check (11 — KR import + `$in` fetch + company_id scope + cancelled exclusion + Map build + embedded fallback retained + resolveKRs adoption at root/dept/team); Express boot; Scenario A (collection-only, **proves migration** for all 3 levels); Scenario B (embedded-only, **proves HYBRID fallback** for all 3 levels); Scenario C (dual-write — collection wins, no STALE-EMBEDDED leak). PX-1.11 verification doc updated: `cascade.js` NOT-MIGRATED → HYBRID ✓ #211; counts 6+2+2 → **5+3+2**. Memory-rule pre-validation: read both `Objective.js` and `KeyResult.js` `required` graphs upfront — Objective fixture first-run pass; KeyResult fixture first-run pass; only fixture cost was discovering `parent_objective_id` and `cascade_level` are NOT formal Objective schema paths (route queries them anyway), so test seeds them via `Objective.collection.updateOne` raw write with `parent_objective_id` stored as STRING (mirrors route's URL-param string filter; existing pre-migration cascade quirk — outside PX-3.6e scope). Regression sweep all green: 8 suites · S24 #243 197/197 + S22a #184e 36/36 + S24 #241 73/73 + S25 #209 36/36 + S25 #210 PX-2.8 55/55 + PX-2.9 32/32 + PX-3.6g 20/20 + S25 #211 PX-3.6e 30/30 = **479 assertions, 0 failed**. Commit `6e50318`. **Critical Why-first audit (Q3 PX-3.6e/d HYBRID-vs-MIGRATED decision)**: per memory `Why → What → How → When` + `Carry skepticism toward "build new" framings`, audited every `Objective.key_results[]` write site BEFORE coding. Found **3 dual-write paths** (POST `/api/objectives` ✓ at [objectives.js:225/249](../../../server/routes/objectives.js#L225); POST `/api/objective-wizard/finalize` ✓ at [objective-wizard.js:722/753](../../../server/routes/objective-wizard.js#L722); POST `/api/key-results` ✓ at [key-results.js:85](../../../server/routes/key-results.js#L85)) vs **8 embedded-only write paths** (PUT `/api/objectives/:id` at [objectives.js:517](../../../server/routes/objectives.js#L517) — collection goes STALE on edit; 5 ai-okr.js cascade-acceptance paths at lines 640/1015/2299/2537/3037 — collection NEVER gets KRs at all; cascade-engine.js at L300/329; objectiveService.js at L499; plus Objective model instance methods at L457/466 mutating embedded directly). HYBRID is **mandatory today** (not just safer-feeling) — dropping fallback would return empty KRs for AI-cascaded Objectives + stale data for edited Objectives + missing KRs for cascade-engine sub-objectives. Audit rationale captured in [REFINEMENT-BACKLOG/README.md](../REFINEMENT-BACKLOG/README.md) Day 4 (#211) section as candidate for "Cross-codebase field-write symmetry audit (FE + BE)" refinement-track work. **PX-3.6d (`consultant.js`) + PX-3.6f (`planning.js`) resequenced — discovered FE-coupling hazard mid-session**: both routes use `objective_id.key_results.id(key_result_id)` (FE-side mongoose subdoc `.id()` lookup) to resolve KR titles for Goals. Hazard: `KeyResult.insertMany(krDocs)` in the 3 dual-write sites does NOT pass `_id: kr._id`, so embedded subdoc `_id` and collection `_id` are **different ObjectIds for the same logical KR**. If consultant.js/planning.js populate is switched to collection KRs today, FE FK-lookup by embedded `_id` (which is what `Goal.key_result_id` was historically populated from) fails. cascade.js + moves.js are not affected (no FE FK lookup by KR `_id`). User picked Option A (defer): new firing task **PX-3.6i — KR `_id` pass-through in 3 dual-write sites** added to S25 Phase 3 scope (~10 min mechanical change at [`objectives.js:233`](../../../server/routes/objectives.js#L233), [`objective-wizard.js:741`](../../../server/routes/objective-wizard.js#L741), and any third dual-write site that materializes from PX-3.6a/b/c work). After PX-3.6i ships, PX-3.6d + PX-3.6f become mechanical (mirror PX-3.6e exactly). Sprint scope: 48 firing → 49 firing (added PX-3.6i; PX-3.6d/f stay in scope, just resequenced). PX-3.6d/f are NOT in REFINEMENT-BACKLOG — they're still S25 work, just gated. **Quality reflection: 9/10**. Strong: surfaced 4 Why-first questions (Q1 sequencing · Q2 bundle vs ship-one · Q3 HYBRID vs MIGRATED · Q4 test shape) before coding (per `Why → What → How → When`); audited write symmetry BEFORE deciding on fallback (skepticism applied symmetrically — not just "build new" but also "drop now"); paused on PX-3.6d when FE-coupling discovered (`stay grounded — minimal change` — admitted PX-3.6d isn't mechanical the way PX-3.6e/g were); resequenced via tiny new task (PX-3.6i) instead of expanding PX-3.6d scope; PX-3.6e fixture first-run pass on Objective + KeyResult schemas (memory rule honored). Negative: cascade route's pre-existing `parent_objective_id` casting quirk (string vs ObjectId, no schema path → no Mongoose cast) cost one debug iteration in test setup; could have been caught by reading the route end-to-end before writing the test seed helper. **Audit IDs**: no per-sub-task A-ID flips this session (PX-1.11 macro is `A20260506-09`, already ✓ Plan + ✓ Code from #209). **Next session**: `/coding` Day 5 — recommend **PX-3.6i** first (10 min, unblocks PX-3.6d + PX-3.6f), then **PX-3.6d** (mechanical after PX-3.6i), then **PX-3.6f** (mechanical after PX-3.6i). After PX-3.6i + d + f all ✓, only PX-3.6h (`weekly-goals.js` HYBRID → MIGRATED) and the 3 write-side migrations PX-3.6a/b/c remain before PX-3.6 macro + PX-3.18 schema-field drop become safe.

**Day 3 addendum — PX-3.6g `moves.js` KR-read migration shipped 2026-05-08 (Session #210)**: First of the 8 PX-3.6a-h consumer migrations locked Day 2 #209. Three embedded reads in [`server/routes/moves.js`](../../../server/routes/moves.js) (lines 67/84/98 pre-migration) replaced with collection-first reads + per-objective embedded fallback (HYBRID until PX-3.6 macro drops fallback for all routes). New `KeyResult.find` joins `Promise.all`; `krById` Map enables O(1) lookups in `resolveMoveParent` (replaces O(N×M) embedded scans). MoveDashboardService unchanged — `deriveObjectiveLifecycle` already had embedded fallback so passing collection KRs is a no-op upgrade. PX-1.11 verification doc ([SPRINT_X_VERIFICATION.md](../../2-TECHNICAL/SPRINT_X_VERIFICATION.md#consumer-migration-status)) updated: 7 NOT-MIGRATED + 1 HYBRID + 2 MIGRATED → **6 NOT-MIGRATED + 2 HYBRID + 2 MIGRATED**. New regression test [`scripts/test-sprint25-PX3.6g-moves-kr-migration.js`](../../../scripts/test-sprint25-PX3.6g-moves-kr-migration.js) **20/20 ✓** in 5 sections: static source check (KR import + krById map + collection-first build + embedded fallback retained); Express boot + GET `/api/moves/dashboard-summary` against three scenarios — Scenario A (collection-only KRs, **proves migration**), Scenario B (embedded-only KRs, fallback still works), Scenario C (dual-write — collection wins; embedded "STALE" never leaks into response). Memory-rule pre-validation: read Objective `required` (end_date + category-default + created_by + owner_id + target_year) and embedded KR `required` (title + metric_type + target_value + quarter + year) from schema before fixture build — caught two iterations before final pass. Regression sweep all green: S24 #243 197/197 + S22a #184e 36/36 + S24 #241 73/73 + S25 #209 36/36 + S25 #210 PX-2.8 55/55 + S25 #210 PX-2.9 32/32 + S25 #210 PX-3.6g 20/20 = **449 assertions, 0 failed**. **Pattern established for PX-3.6a-f**: (1) require `KeyResult` model, (2) add to `Promise.all` parallel fetch, (3) build `krById` + `krsByObjective` from collection + per-objective embedded fallback, (4) use `kr.objective_id` directly instead of scanning embedded arrays, (5) test = static check + Express + 3 scenarios (collection-only/embedded-only/dual-write).

**Day 3 — PX-2.8 + PX-2.9 shipped, PX-2.7 deferred 2026-05-08 (Session #210)**: 2 code tasks shipped + 1 task re-prioritized to refinement backlog (user call: "why retire now — can't we do it later, optimization work can be done after sprint 27"). PX-2.7 deferral rationale: pure deletion of dormant `ssi-questions-library.json` seed path; no Beta flow triggers `seed:questions`; the actual journey-blocker (`A20260508-01` MECE seeder default) shipped Day 1 as PX-2.6; Day-1 RETIRE strategy decision still stands when refinement track picks it up; ~30 min of work parked. Captured in [REFINEMENT-BACKLOG/README.md](../REFINEMENT-BACKLOG/README.md) "Sprint 25 Day 3 (#210) deferrals" section. Sprint scope 49→48 firing tasks. **PX-2.9 ship**: new [`server/models/AssessmentTemplate.js`](../../../server/models/AssessmentTemplate.js) fields `archived_at` + `archived_by_user_id` (default null, indexed); new [`server/services/OrphanArchiveService.js`](../../../server/services/OrphanArchiveService.js) (`archiveTemplatesForUser` + `archiveTemplatesForCompany`; idempotent via `archived_at: null` filter; flips `is_active: false`; errors logged not propagated); pre-delete hooks on [`User.js`](../../../server/models/User.js) + [`Company.js`](../../../server/models/Company.js) covering BOTH document middleware (`doc.deleteOne()`) and query middleware (`Model.findByIdAndDelete` / `Model.deleteOne({_id})` / `Model.findOneAndDelete({_id})`). Day-1 #208 ARCHIVE strategy decision honored — never hard-deletes; preserves audit trail. Audit `A20260508-06` flipped 📝 → 💻 ✓. New regression [`scripts/test-sprint25-PX2.9-orphan-archive.js`](../../../scripts/test-sprint25-PX2.9-orphan-archive.js) **32/32 ✓** in 9 sections covering: schema fields present + defaults + ref; `archiveTemplatesForUser` archives matching only (others untouched); idempotency (re-archive no-op, archived_at unchanged); `archiveTemplatesForCompany` with actorUserId; `User.findByIdAndDelete` triggers query-middleware hook; `userDoc.deleteOne()` triggers document-middleware hook; `Company.findByIdAndDelete` triggers hook with `archived_by_user_id: null` (Co-driven); null-ID error guards. Regression sweep all green: S24 #243 197/197 + S22a #184e 36/36 + S24 #241 73/73 + S25 #209 36/36 + S25 #210 PX-2.8 55/55 + new PX-2.9 32/32 = **429 assertions, 0 failed**. Memory-rule pre-validation again: read `User.pre('save')` requires `company_id` despite schema-level `required:false` → fixture creates Company first, then User; first-run pass.

**Day 3 — PX-2.8 onboarding state machine wiring shipped 2026-05-08 (Session #210)**: Strategy decisions locked (Q1 latest-assessment-wins · Q2 any-goal-counts · Q3 default 0/40/60/80 readiness cutoffs). New service [`server/services/OnboardingProgressService.js`](../../../server/services/OnboardingProgressService.js) (3 entry points: `onAssessmentCompleted` → rollup `assessment_scores` + tick `onboarding_progress.assessment_completed` + flip `Company.stage` prospect→onboarding + preserves S22a #184e first-completion history marker; `onObjectiveCreated` → tick `first_objective_created`; `onGoalCreated` → tick `first_goals_assigned` + flip onboarding→active; all idempotent via false-state filter or `fromStage` filter; errors logged not propagated). Wiring at 5 route sites: [`routes/assessments.js:343`](../../../server/routes/assessments.js#L343) (replaced bare history-only call); [`routes/objectives.js:262`](../../../server/routes/objectives.js#L262) + [`routes/objective-wizard.js:766`](../../../server/routes/objective-wizard.js#L766) (added tick alongside existing `onFirstObjectiveCreated` flip); [`routes/goals.js:927`](../../../server/routes/goals.js#L927) quarterly POST + [`routes/goals.js:1443`](../../../server/routes/goals.js#L1443) legacy weekly POST + [`routes/weekly-goals.js:289`](../../../server/routes/weekly-goals.js#L289) canonical WeeklyGoal POST. Architecture decision: route-layer wiring (matches existing 5/5-callers pattern) NOT model post-save hooks — model hooks would leak into seeds/migrations/tests. Audit `A20260508-05` flipped 📝 → 💻 ✓. New regression test [`scripts/test-sprint25-PX2.8-onboarding-progress.js`](../../../scripts/test-sprint25-PX2.8-onboarding-progress.js) **55/55 ✓** in 8 sections: module exports; `readinessProfileFor` 10 boundary mappings; `rollupFromAssessment` payload shape (null on non-SSI; correct $set keys + values); first SSI completion (rollup + tick + flip + first-completion history marker); retake idempotency (rollup overwrites latest-wins, NO re-tick, NO re-flip, NO duplicate marker); objective creation tick + idempotency; goal creation tick + flip + idempotency; null-ID error guard. Regression sweep all green: S24 #243 197/197 ✓ + S22a #184e 36/36 ✓ + S24 #241 73/73 ✓ + S25 #209 36/36 ✓ + new #210 55/55 ✓ = **397 assertions, 0 failed**. Per memory `feedback_test_fixture_validation`: pre-validated `Company` (only `name` required) and `Assessment` (`company_id` + `assessment_type`; user_id optional with anonymous_respondent fallback) before seeding — first-run pass on test, breaking the 8-session fixture-cost streak.

**Day 2 — Phase 1 sealed + 2 code tasks shipped 2026-05-09 (Session #209)**: All 11 Phase 1 verification tasks executed via 3 parallel Explore agents (Cluster A: PX-1.5/1.6/1.7 · Cluster B: PX-1.2/1.8/1.9/1.10/1.11 · Cluster C: PX-1.1/1.3/1.4). PX-1.6 gate **🟢 GREEN** — all 4 S24 prereqs verified shipped (commits `6456832`/`90b0a25`/`106d095`/`d34d753`). Phase 2 conditional decisions: ~~2~~ **1 fire** (PX-2.2 ✅) · 2 defer (PX-2.1, **PX-2.5 demoted Day 2** — wizard-session-bound endpoint not usable for saved Objectives, needs new BE+FE+tests ≈2-3h, deferred to own session) · 2 not needed (PX-2.3, PX-2.4). Phase 4 conditional decisions: 1 fire (PX-4.3 ✅ retired `OKRRecommendationService` dead code + cleaned `testSSIDiagnostic.js`) · 1 defer (PX-4.1) · 1 keep (PX-4.2). **Code shipped**: PX-4.3 (`server/services/diagnostic/OKRRecommendationService.js` deleted, 438 LOC; `server/scripts/testSSIDiagnostic.js` cleaned of import + Test 5 section); PX-2.2 ([`server/services/LifecycleTransitionService.js`](../../../server/services/LifecycleTransitionService.js): module-scoped `notifyTransition({service, docId, fromStage, toStage, actor, triggeredById, triggeredByKind, hasNote})` async helper + call site inside `_record()` flip path immediately after telemetry emit, fire-and-forget `.catch()` pattern, exported for spy-testability per cross-sprint audit Group 2a). New regression test [`scripts/test-sprint25-209-notify-transition.js`](../../../scripts/test-sprint25-209-notify-transition.js) **36/36 ✓** covering: helper exported + async; payload shape (8 fields exactly); fire on flip path (companyStage + objectiveLifecycle); NOT fire on idempotent no-op; NOT fire on history-only entry; manualTransition catalog flips fire; thrown errors swallowed + logged not propagated; default ship is a no-op. Regression: S24 #243 197/197 ✓ + S22a #184e 36/36 ✓ + S24 #241 73/73 ✓ ≈ 342 assertions green. **Critical**: PX-1.11 found 8 not-migrated consumers of embedded `Objective.key_results[]` → user locked **PX-3.6a-h as 8 separate IDs** (sprint scope 47→55, then 55→55 net after PX-2.5 demotion → 49 firing). Refinement-track items captured for PX-1.2/1.8/1.10 + KR aggregation cron (S27 owns). Deliverables: [SPRINT_X_VERIFICATION.md](../../2-TECHNICAL/SPRINT_X_VERIFICATION.md) (sealed ACTIVE; PX-1.4 finding patched to reflect wizard-session-bound endpoint reality) + [KR_AGGREGATION_AND_LIFECYCLE_AUDIT.md](../../2-TECHNICAL/KR_AGGREGATION_AND_LIFECYCLE_AUDIT.md) (new ACTIVE).
