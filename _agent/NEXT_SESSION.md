# Next Session

> Written by the previous session per `/close`. `/init` reads this and starts. Overwritten every session — history lives in JOURNAL.md.

**Type**: coding (mockups — session 3 of 3, then founder review checkpoint) — **with a Level-0 gate**
**Task**: `N1-P2-08` — Teams + Planning mockups, Builder variant, Profile player card

**Gate**: `gh pr list` first.
- **PR #19 (mockups s2) merged** → proceed with session 3 below.
- **PR #19 still open** → pick **`N1-P3-01`** — populate 0-BUSINESS and 1-PRODUCT (M, READY, last independent item; DoD: 4 files each in 0-BUSINESS/ and 1-PRODUCT/, leads with Transformation OS, AI Readiness as launch vertical, both GTM motions, Karvia adaptations cited; draw NORTH_STAR, AI_CONSULTING_PLAYBOOK, BOQ_FRAMEWORK, PRODUCT_STRATEGY; note: several candidate files may overlap docs that already exist — pre-scan what 0-BUSINESS/ and 1-PRODUCT/ already hold and define the 8 files to ADD, not duplicate; if the split is ambiguous → clarifications and pick next).
- If BOTH #19 and an N1-P3-01 PR are in flight → remaining independent: none — journal a no-op with the wait reason.

**Session 3 scope (when #19 is merged)**:
- `teams.html` — people fabric per contract: Invite member primary CTA, tiles (onboarded vs invited, per team, assessment participation), empty state "objectives need owners"
- `planning.html` — Worker home per contract: Add milestone/task CTA, current-milestone tiles (done/planned, hours, streak), milestone→task board reading as "what do I do today", empty state "pick a KR"
- `dashboard-builder.html` — the mode flip: same tiles, program = the application being built; My Clients absent from nav
- `profile.html` — the player card: skills/motivations/interests as **tags never prose**, role-per-program, assessment-fed fields marked
- New components → DESIGN_LANGUAGE first; mockups/README table updated
- **Then**: flip BACKLOG N1-P2-08 → NEEDS-FOUNDER-REVIEW; the review unblocks N1-P4-01

**Cards to draw**: PRODUCT_STRATEGY §§ Teams/Planning + § player cards + § fit thesis · NOF.md · DESIGN_LANGUAGE component set · shell.css · tokens.css

**Definition of done (s3)**: 4 HTML files per contracts; Profile is match-grade; hex scan = anchors only; tokens all exist; README updated; validator green.

**Watch out for**:
- Planning must read like "what do I do today", not a Gantt chart
- Level 0: open PR, don't merge; never stack onto an unmerged branch
- Morning human TODO: merge #19 (review `assessment-flashcards.html` — PQ-4 direction + alternatives) and tick-16's 4-CUSTOMER PR
