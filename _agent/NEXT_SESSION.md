# Next Session

> Written by the previous session per `/close`. `/init` reads this and starts. Overwritten every session — history lives in JOURNAL.md.

**Type**: STRATEGY (design/mockups — Night 1b, sprint 1 closer)
**Task**: **N1-P3-09** — C-013 brand & layout rework (2 steps; may span 2 sessions). Why now: it's the last sprint-1 task (P3-01 and P3-08 are DONE); sprint 2's contracts (N1-P4-01) open once sprint 1 closes, and the mockups must reach the ratified two-tier brand before the close groom re-runs the verification gates.

**READ FIRST** (in order):
1. `_agent/SPRINTS_NIGHT_1B.md` — this task's spec (tokens.css v2: Sora/Manrope/Cormorant, teal + gold; DESIGN_LANGUAGE two-tier rework; sidebar shell across all 10 mockups; stage-badge text sweep per audit 2.8)
2. `_agent/DECISIONS.md` C-013 — the founder's brand verdict this task executes ("the review verdict is this rework")
3. `NEXUS_STRATEGY/1-PRODUCT/design/DESIGN_LANGUAGE.md` — the doc being reworked (and the bedside-manner/display doctrine it must keep)
4. The current `tokens.css` + the 10 mockup files (verify paths via `1-PRODUCT/design/README.md`) — ground the spec against what exists before editing
5. `_agent/AUDIT_2026-06-12.md` finding 2.8 — the stage-badge text sweep (Engaged/Assessing/Handed over → constitution §4 badge names: Prospect · Measuring · Aligning · Transforming · Evolving)

**Definition of done** (from the sprint spec):
- tokens.css v2 ratified palette/type in place; DESIGN_LANGUAGE reworked two-tier; sidebar shell on all 10 mockups; stage-badge text sweep done
- Verification gates re-run and green: hex scan zero (tokens before mockups — SESSION_PRACTICES rule 3), token-existence check, contract check, doc-graph green

**Watch out for**:
- **The PR stack is now 5 deep** (#25 ← #26 ← #27 ← #28 ← session-26 PR). If all merged: branch from main. If not: continue the chain off `session/2026-06-12-26-trigger-map-playthroughs` — do NOT base on main
- This is a 2-step task — if it splits, seal step 1 (tokens + DESIGN_LANGUAGE) as its own session and carry the mockup sweep; don't chain "just one more" past budget
- Tokens before mockups: define every CSS token first, then reference it; no inline hex anywhere (rule 3)
- The badge sweep is text-only canon alignment — don't redesign badge components while sweeping
- P3-08 landed fallback cadences (48h/day-3/day-7 in the trigger map) as agent judgment — if the founder is in-session, a 30-second confirm closes that flag (JOURNAL session-26 quality note)
- Step count: 31/105 after session-26's journal entry (Night 1b: 2 of 10 spent)
